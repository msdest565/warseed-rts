# R4/R5 接手入口

当前状态仅以AI_DEVELOPMENT_STATE.md控制块为准。范围限R4/R5，禁止自行进入R6/R7。

## 已完成与当前任务

- R4-001～006均DONE；D-029出口8/9目标完成，两种方案各覆盖三种敌方计划，18场重复一致。完整门819.106秒，提交ab94ea7已推送。局部目标完成不等于整局胜利，详情见R4_EXIT_EVIDENCE.md。
- R5-001 DONE，提交99d43c6已推送。typed准则与行动阶段、隐藏总部真值读取修复、真实撤退、值拷贝和玩家知识隔离完成；完整门765.543秒通过，719文件无漂移。
- R5-002 DONE，完整门815.577秒、724文件无漂移。灰脊14敌军重新分为8主攻、2试探、4条件预备。最终专项r5-002-focused-final2.log及完整门r5-002-release.log通过，冻结r5-002-frozen.json。003已DONE（完整门1046.924秒、752文件无漂移）；004 DONE（960.707秒完整门、最终ui5五档及导出/包验证）；005 READY。

## 读取最小集

AGENTS.md → 状态控制块 → AI_DEVELOPMENT_WORKFLOW.md验证矩阵 → 当前工作项 → ENEMY_OPERATION_GUIDE.md → 相关代码。不要重复读取庞大历史状态全文。

核心接口：EnemyOperationDefinition.compile_legacy负责旧typed内容的兼容；EnemyOperationSystem由世界持有、冻结并推进；EnemyOperationSnapshot递归复制资源。EnemyReservePolicy和EnemyReserveEvaluator分别定义条件与纯合法快照判断。正式命令继续同一管线；世界不增加地图ID分支。

002的关键边界：120tick前不释放；减员、当前可见接触或指定已完成阶段允许释放；普通编队投入上限与累计预备释放上限同时成立；显式待命预备不能被其他阶段绕过；撤退锁存后不再释放。黑井旧有定时预备属于兼容内容，不得误当未分配编队拒绝整份目录。

## 剩余任务，按依赖执行

| ID | 范围 | 必须保留的验证 |
|---|---|---|
| WS-R5-003 | 三种实际不同的多轴/牵制/欺骗/补给切断模板；使用typed阶段与数据，不只改延迟 | 轨迹、目标与投入实际不同；补给节点失守确实减少收入；完整对局及重复结果 |
| WS-R5-004 | 敌方反应事实、规则、延迟、成本和结果审计；合法双语复盘 | 隐藏单位/经济/总部污染不改提案；更换目标须重新累计反应延迟；旧情报不实时追踪；玩家复盘不暴露未获知信息；五档UI |
| WS-R5-005 | 阶段出口审查 | 前置全部DONE，三计划×多策略、知识隔离、确定性、存档、UI、导出全部通过；性能D-028暂缓；HUMAN可选 |

现有反应代码位于simulation_world.gd的_enemy_reaction_observation/_commit_enemy_reaction；需要核对观测目标改变时的延迟重置。战后入口为after_action_review_projector.gd和battle_debrief.gd。不要把命令接受记录写成已经执行或达成目标。

## 基准更新与已排除的问题

R5-001黄金漂移来自撤退与阶段日志；隔离反事实恢复全部18个旧黄金。R5-002漂移来自兵力重排与条件预备；隔离恢复12+2后全部18个001黄金恢复。灰脊矩阵现在同时检查黄金和第二次运行。失败日志保留，不能只刷新哈希。

集中进攻代理曾停在未获总部视野的接近点。现已增加基于合法己方位置的前探，并计入纠正预算，禁止重新把隐藏总部坐标喂给代理，也禁止降低胜负门槛来掩盖问题。

## 费用与低成本委派

主模型累计上限1000美元，输入10美元/百万、输出50美元/百万，Luna排除；按全部输入计费的本地保守估算不等于账单。artifacts/check-r4-r5-budget.py的基线input292997349/output927181不得重置。970美元停止新增实现，保留最后验证与交接预算；实际最新值看artifacts/r4-r5-budget-latest.json。

Luna资源定义草稿可用；较复杂判断器首次捏造多个API名称，已退回修正后由主代理本地验证。原始错误稿不是有效代码。外部请求无自动重试，长输出可能超时。接口只用于开发辅助，核心游戏/内容加载/测试不得依赖网络。不要打印凭据或提交本机配置。

先前暂停时保守费用约371.95美元；以budget-latest.json更新值为准。已完成R4和R5-001/002，R5-003/004已验收，005出口审查待执行。不要重跑已通过门消耗剩余额度，也不要因接手而重置计费基线。
