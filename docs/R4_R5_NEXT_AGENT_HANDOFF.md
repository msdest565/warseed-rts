> 当前：R4-006已DONE，完整门819.106秒，D-029指标8/9与两方案全覆盖均通过；见R4_EXIT_EVIDENCE.md。R5-001已READY。下文R4研究和阻塞描述仅作历史，不重新实施。

> 最新：D-029已接受出口门槛，预算400美元且基线不重置；下文研究失败仍保留。

# R4/R5 后续代理接手指南

本文件是接手导航，实时任务以 AI_DEVELOPMENT_STATE.md 为准；不要据本文件提前宣告阶段完成。性能要求按D-028暂缓，不能据此跳过功能或产品门。

## 最短恢复步骤

1. 读 AGENTS.md、状态控制块与第22节、当前工作项；运行 git status --short、git diff --check。
2. R4-004 已提交并推送23aa5a8，六阶段实现、8场确定性和694.338秒完整门通过；不要重新实现。
3. R4-005 最终状态需回读控制块。源码在 commander_adaptation_agent.gd（纯提案）、commander_adaptation_system.gd（权威动作）、commander_task_graph_system.gd（父命令应用复验）。所有四类行为已有真实执行专项；测试入口 tests/tools/commander_adaptation_smoke.gd。
4. 用户尚未冻结R4-006计划完成率门槛时，不进入R5。已提出建议：9个组合至少6个完成批准目标且两种方案各覆盖三种敌方计划，纠正≤2次/10分钟，确定性与知识隔离全部通过。现已由用户接受为D-029（2026-09-16）。现有每10分钟纠正上限来自路线图，其余量化解释待用户确认。
5. 如用户继续，保留其最新决定与累计400美元主模型累计上限；不能每次接手重置计费基线。

## R4-005 已证明的行为与边界

- 批准请求的max_supply_cost减去原部署承诺，作为本图后续补员/预备投入总额度。普通Supply、人口、部署和共享冷却继续校验；预算记录为承诺额度，部署最终失败也不偷偷返还重花。
- 纯Agent只读阵营合法快照；create_commander_task_snapshot新增的经济只包含本方。父系统在每次应用前重新验证修订、当前资格和普通支援规则，不是只在入队前验证。
- 每次最多应用一个调整，图修订递增阻止同批重复；重规划和撤退重规划分别最多2次，间隔30tick，预备最多1卡、补员最多2次。42%与75%是可调工程默认值。
- 撤退锁存至下一次玩家批准，手控/RETURNING受保护；明确接管提交移除同卡待执行自动调整。不能把图PAUSE误认为AI抢手控。
- 无武器部队执行普通移动；不要将补给车投入失败掩饰为预备从未实际部署。
- 前置任务恢复使依赖阻塞节点恢复等待，不消耗额外寻路重试，不复活玩家取消节点。
- 8场定向审计与五档UI均有日志。完整门及最终哈希以工作项末尾为准；失败日志必须保留。

## R4-006 尚待完成

工作项范围：冻结出口标准、完成率/纠正负担对局代理、因果原因覆盖、公平知识与重复确定性、发布证据审查。不能把单次入队、任务类存在、0次脚本输入或方案局部完成写成整局获胜/无需纠正。

只读研究 artifacts/r4-exit-research.json（脚本 artifacts/r4-exit-matrix-draft.gd）已有9场：集中投入三种敌方均完成中央批准目标；侧翼推进在central_assault与western_feint完成，在western_hook未完成；侦察先行三例未完成。准确完成率5/9，9场均自然结束。所有结果是SIMULATED；这些场次的battle_result均为失败，不能把局部目标完成写成战斗胜利。该代理只批准一次目标、没有后续决策，不充分代表正常整局决策流程，也不是冻结的出口验收脚本。

下一位代理先检查失败组合western_hook/flanking_advance的阶段和合法知识轨迹，再决定是否属执行缺陷、方案风险或代理场景限制。不要为抽到PASS反复跑矩阵，不在观察结果后偷偷降低阈值。对需要纠正的阻塞必须记录来源、时刻、执行命令及是否恢复；单纯不下任何命令而报告0纠正没有证明力。

## R5 工作包准备（全部依赖未解锁，不是已实施）

| 工作项 | 核心行为 | 直接代码入口 | 最小验收 |
|---|---|---|---|
| WS-R5-001 | typed Doctrine与分阶段Operation Plan，在读取本局玩家行为前锁定主攻轴、目标、预算、撤退条件；世界仅持有状态/调度 | battle_enemy_plan_definition.gd、simulation_world.gd的_select_enemy_opening_plan与_setup_grey_ridge_scenario；AI设计第13节 | 锁定后修改隐藏玩家状态不改变开局；typed加载非法值/引用失败；真实阶段转换；统一命令 |
| WS-R5-002 | 稳定编队分配、投入上限与预备释放条件 | 战斗enemy_formation角色、既有合法反应规则与命令 | 至少两个职责/目标；预备在触发前保留；资源与卡/编队不得重复分配；死亡/延迟/应用复验 |
| WS-R5-003 | 至少三套可观察不同的多轴、牵制/欺骗、补给切断计划 | data/battles、BattleEnemyPlanDefinition、独立敌方行动系统 | 路线/目标/资源实际不同，不只改延迟；切断须造成真实补给影响；跨三种玩家策略完整结束与重复指纹 |
| WS-R5-004 | 每次改令有已观察事实、规则ID、反应延迟、投入成本、结局归因；玩家仅见合法复盘 | EnemyReactionSnapshot与enemy_reaction_log、debrief消费者 | 隐藏位置/经济/未侦察卡变化不改变提案；过期情报不变成实时追踪；中英复盘与五档 |
| WS-R5-005 | 三计划×多策略阶段出口，迁移与发布审查 | 前四项工作包和完整发布脚本 | 依赖全部DONE，功能/公平/确定性/存档/UI/导出证据通过；产品决定按当前授权与实际门禁处理 |

兼容策略：保留Legacy RTS EnemyRaidAgent路径；R5主要改正式部队卡会战的开局/后续行动。既有BattleEnemyPlanDefinition已有主攻/试探/固定后续字段，不能误称完全没有敌方计划。不要继续在SimulationWorld添加新的doctrine ID或地图特判，应抽出独立typed系统。重点审计现有_advance_grey_ridge_offensive_followup对玩家总部权威对象的访问；必须区分公开地图目标、合法已观察总部信息与隐藏真值，不因目前固定坐标就认定公平。不要未经迁移说明替换全部四关内容或更新全部黄金。

## 成本与Luna边界

主模型按用户输入10美元/百万、输出50美元/百万；保守计全部输入、未计缓存折扣。artifacts/check-r4-r5-budget.py依据本会话本地token记录差值，基线input292997349/output927181，不得重置。370美元停止新增实现、留30美元验收交接。Luna费用排除。

Luna文本接口适合短而完整的契约/查找；长代码输出两次超时，不自动重试。原生代理responses接口本次返回502/Upstream access forbidden，外部chat/completions仍可用。r4-005-review-01三项发现全部忽略了已说明的父系统应用复验/修订锁，经核对不成立；不能把该轮写成审查PASS。凭据只从本机配置指向的文件读取，禁止打印或提交；代码、战斗、测试不依赖服务可用性。
