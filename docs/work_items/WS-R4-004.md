# WS-R4-004：将领阶段任务图

```yaml
work_item_id: WS-R4-004
title: Execute approved staff plans through staged whole-card tasks
phase: R4
type: authoritative_vertical_slice
status: BLOCKED
owner: ai
external_research: optional
objective: 批准方案按逐卡依赖图实际执行，阶段状态、目标、完成/失败和撤退原因可从合法快照追溯。
why_now: R4-003已完成并推送f4cc93c，当前批准记录没有任务执行消费者。
depends_on: [WS-R4-002, WS-R4-003]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DESIGN_AND_DECISION_TREES.md, docs/TECHNICAL_PLAN.md, docs/work_items/WS-R4-002.md, docs/work_items/WS-R4-003.md]
current_evidence: [R4-003完整门665.333秒PASS, 五档中英PASS, 工作区预检干净, _assign_unit_card_task已支持整卡任务但原CommanderOrder会重派全部下属]
in_scope: [typed阶段图契约与校验, 批准方案消费, 逐卡命令校验/应用, 合法快照阶段评估, 依赖/完成/失败/显式撤退, 值拷贝快照与双语UI, 专项/完整门]
out_of_scope: [R4-005的自动预备投入/增援决策/损失撤退策略/受阻重规划, R4阶段出口纠正负担调优, 敌方计划AI, 跨局存档]
invariants: [10Hz权威推进, Agent只读本方合法快照, 所有新任务请求经过统一队列和应用复验, 整卡操作, 不抢手控或归队卡, 不通过将领全体指令替代逐卡方案]
behavior_contract:
  inputs: R4-003权威批准的StaffCourseOfAction；typed阶段模板；逐卡阶段命令；显式撤退命令
  authoritative_changes: 批准后建立本局任务图，稳定图/节点ID关联原方案；逐卡节点依赖完成后才可入队，应用再次校验；任务由世界系统持有，Agent不直接写阶段或部队
  rejection_reasons: INVALID_TARGET、NOT_CONTROLLER、AGENT_NOT_AUTHORIZED、TASK_CONFLICT、AGENT_OVERRIDE_BLOCKED、PATH_UNAVAILABLE、BATTLE_CONCLUDED；失效或篡改的图/节点请求无副作用
  events_and_snapshots: typed图/节点快照包含phase/lifecycle/prerequisites/card/commander/task/source_plan/tick/reason；阶段变化有结构化事件，本方隔离与深复制
data_contracts:
  - 阶段MUSTER、RECON、DEPLOY、ENGAGE、EXPLOIT、RETREAT使用受限枚举和typed Resource，不用脚本表达式；前置引用、环路、重复ID、超时和完成窗口均加载校验。
  - 集结考虑已投入同将领部队与准备延迟；侦察阶段由真实侦察分工执行，主力等待侦察依赖；无侦察分工时显式跳过而非伪造侦察成功。
  - 展开执行已批准战略路标并逐段经过现有寻路校验；交战依赖合法可见敌情和公开地区目标；利用在目标已取得且本卡实际到位后持续保持完成窗口。
  - 预备卡仅按批准部署投入消费Supply，保留卡不被后台旧将领策略投入；部署等待权威成员生成，不能以入队视为完成。
  - 新批准方案显式替换旧图；玩家新的将领决策优先并取消对应旧图分工；接管使所属节点暂停，主动交还且归队完成后才能恢复，不修改其他卡。
  - 004提供明确撤退入口和RETREAT节点到安全集结区的完成/失败条件；自动损失阈值与重规划策略由005实施。
  - 阶段不以前进tick单独冒充成功；无法到达、任务失败、部队覆灭或超时需稳定BLOCKED/FAILED原因；队列去重且同tick不越过未应用的依赖。
  - 没有批准方案时旧四关及Legacy行为不变；战斗结束后图冻结，重开清空。
persistence: {format_change: false, migration: none}
observability: [逐卡阶段进度与原因, 原方案指纹, 任务ID, 阶段事件, 确定性执行轨迹]
acceptance_criteria:
  - 从正常比较入口批准后产生真实逐卡任务，投入/路线/准备与原方案一致，预备卡保留；至少两种方案执行轨迹可区分。
  - 六阶段前置/完成/失败可达；侦察先行、主力等待、明确目标完成及显式撤退均有纯模拟和整局证据。
  - 同tick接管/替换/重复命令、失效节点、预算不足/部署等待/卡损失/坏路线保持合法可解释行为。
  - 双方知识污染与旧快照不可变、确定性换序、统一命令回归通过；五档中英显示阶段，60/80性能及完整发布门通过。
verification:
  baseline: [git status --short, git diff --check, R4-003完整门PASS]
  focused: [阶段模板校验, 逐卡图执行专项, tests/test_runner.gd, 灰脊批准方案完整运行, 五档中英实窗]
  full_gate: true
  optional_research: [HUMAN计划理解与纠正负担可选NOT_RUN]
expected_files: [src/data/commander_task_*, src/agents/commander_task_*, src/commands/commander_card_task_command.gd, src/simulation/systems/commander_task_graph_system.gd, src/simulation/core, src/ui, tests, locale]
risks: [已有战法和持续任务可能改写路线或抢先投入预备, 旧任务完成规则不能直接等同方案完成, 路标可达性仍需权威校验]
rollback: 图仅本局内存状态；移除消费入口恢复R4-003，不逆向迁移玩家存档。
handoff_outputs: [阶段执行证据, 正常状态更新, 下一项WS-R4-005]
```

2026-09-13 READY → DISCOVERY → CONTRACT：读取路线、AI任务模型、技术边界及003最终证据；跟踪审批、部署、整卡任务、持续防守、将领自动战法、接管/归队和队列顺序。原StrategicOrder会筛掉工程等非战斗成员，不能直接作为正式混成整卡执行入口；使用独立逐卡命令接入既有整卡任务创建，并对原后台策略显式仲裁。当前仅冻结本工作项，不领取005。

CONTRACT → IMPLEMENTING：先建立可校验的六阶段typed模板、逐卡图值拷贝和从真实批准快照编译的纯函数；随后接入权威逐卡命令、阶段评估、UI和专项。数据编译通过不等于任务已执行，本项不得仅凭图类/测试存在标DONE。


2026-09-13 IMPLEMENTING → VERIFYING：批准消费、真实整卡六阶段任务、前置/完成窗口、部署预算与应用复验、手控暂停/归还、主动撤退及双语阶段展示已实现。审查补齐准备期停止旧任务、保留卡持续归属、失效安装ID隔离、旧自动部署仲裁、任务完成事件，以及显式区分部队损失/撤出/超时/依赖失败。受阻请求经同一队列应用，不由只读Agent直接改权威状态；撤退不抢手控，取消旧任务后交还从当前图恢复。

`artifacts/r4-004-boundaries2.log`专项PASS，`artifacts/r4-004-regression-boundaries.log`18/18 PASS，新增125秒实际tick接管/交还、重复/伪造/越权命令、同tick将领覆盖、方案替换、准备延迟、混成全员、部署等待/预算损失、坏路及依赖阻塞、快照污染和完成事件。首轮boundaries的两个失败为测试构造负started_tick，保留日志并修正为合法非负开始时间；不是放宽阶段超时。三方案×两次完整结束审计、五档中英实窗、活动图60/80实渲与完整发布门待最终验证；仍非DONE。所有证据SIMULATED，HUMAN仍为可选NOT_RUN。

2026-09-14 用户要求先修复手控战斗并交付可玩包，随后暂停开发，本项由 VERIFYING 转为 BLOCKED / USER_PAUSE。现有实现保留并纳入维护包回归；最新图专项 `artifacts/manual-graph-final-focused.log`、18套 `manual-final-regression3.log` 和五档 `manual-shipping-ui.log` 已通过。维护包的最终发布结果统一归档在 `AI_DEVELOPMENT_STATE.md` 第20节及 `WS-MAINT-20260914-001.md`。本包性能例外不替代本阶段后续验收，不将本项标为DONE，不解锁005；恢复开发须由用户明确指示。
