# WS-R2-001：灰脊完整对局基线与因果观测契约

```yaml
work_item_id: WS-R2-001
title: Grey Ridge full-match baseline and causal observability contract
phase: R2
type: vertical_slice
status: DONE_SIMULATED
owner: ai
external_research: optional
objective: >-
  建立不改变现有战斗结果的灰脊完整对局评估基线，使后续玩法、卡牌和 AI 改动能够用完整策略差异、人工纠正负担和权威因果链比较，而不是只看开局 200 tick 与命令是否被接受。
why_now: >-
  当前四关矩阵证明确定性和命令合法，但不能判断委托质量、策略可行性和失败原因；在改动玩法前必须先冻结可重复的比较基线。
depends_on:
  - WS-R1-008
  - D-026
  - D-027
source_documents:
  - docs/PRODUCT_VISION.md
  - docs/GAME_DESIGN_DOCUMENT.md
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/AI_DESIGN_AND_DECISION_TREES.md
  - docs/EXPANSION_CURRENT_STATE_AUDIT.md
current_evidence:
  - tests/scenarios/four_operation_release_balance_audit.gd 主要覆盖短窗口、命令接受、锁定计划和 fingerprint
  - 开发者真实窗口委托自测中四关均失败，属于 SIMULATED/DEVELOPER 基线而非平衡结论
in_scope:
  - 定义完整对局评估指标、因果 reason 和稳定报告 schema
  - 为灰脊建立三个锁定敌方计划乘五种玩家策略的完整时限运行器
  - 记录高层意图、Agent 改令、玩家纠正、整卡接管、资源承诺、任务阶段、关键损失和结算原因
  - 生成确定性 JSON 报告及针对报告契约的自动测试
  - 将现有结果记录为基线，不为获得绿色结果修改战斗数值
out_of_scope:
  - 不调整单位、关卡、战法、AI 策略或胜负数值
  - 不实现战线叠层、参谋方案、混成卡或敌方新计划
  - 不改变存档格式、发布包或其他三关内容
invariants:
  - SimulationWorld 保持 10 Hz 权威推进
  - 玩家与 Agent 继续使用同一命令和校验管线
  - 策略代理只能读取对应阵营可见快照
  - 报告字段是观测输出，不能成为权威输入
  - 固定种子与稳定 ID 排序必须得到相同 fingerprint
behavior_contract:
  inputs:
    - 灰脊场景 ID、敌方计划 ID、玩家策略 ID、固定种子
  authoritative_changes:
    - none
  rejection_reasons:
    - 未知场景、计划或策略 ID 必须明确失败
    - 策略读取非法隐藏状态时测试必须失败
  events_and_snapshots:
    - 只消费已有权威命令结果、事件和快照；缺失的观测字段另行以值拷贝方式补充
data_contracts:
  - 报告 schema 使用稳定字段名和排序，不依赖 Dictionary 遍历顺序
  - 每条因果记录包含 tick、actor/card/task ID、reason key、source event 和结果
persistence:
  format_change: false
  migration: none
observability:
  - 首条有效高层意图 tick
  - 每类 Supply 承诺与累计投入
  - 玩家强制纠正次数和原因
  - Agent 任务创建、替换、阻塞、撤退和完成次数
  - 整卡接管次数、持续 tick 和归还结果
  - 卡牌现员变化、关键损失来源和最终贡献
  - outcome、grade、reason 和关键转折列表
acceptance_criteria:
  - 三个敌方计划与五种玩家策略均运行到权威结算或正式时间上限
  - 至少包含推荐分轴、集中进攻、侦察后投入、保留预备队和无干预策略
  - 相同输入连续两次产生字节等价规范化报告或相同稳定 fingerprint
  - 报告能够区分命令被接受、任务被执行、任务受阻和目标完成
  - 测试证明代理无法读取战争迷雾后的玩家或敌方真值
  - 当前胜败作为基线事实记录，不把策略全胜设为本工作项通过条件
verification:
  baseline:
    - Godot headless editor import
    - 14 套现有 headless tests
    - tests/scenarios/grey_ridge_decision_matrix.gd
  focused:
    - 新完整对局基线脚本
    - 报告 schema、确定性和知识公平单元测试
    - 相同矩阵连续运行两次并比较 fingerprint
  full_gate: false
  optional_research: []
expected_files:
  - tests/scenarios/grey_ridge_full_match_baseline.gd
  - tests/unit/test_gameplay_observability.gd
  - src/observability/
  - artifacts/balance/grey_ridge_full_match_baseline.json
  - docs/AI_DEVELOPMENT_STATE.md
risks:
  - 观测字段意外读取或影响权威状态
  - 完整矩阵运行时间过长
  - 把代理策略胜率误写为真人乐趣结论
rollback: >-
  新观测和运行器应可整体移除而不改变 SimulationWorld 行为；不得为报告修改存档或权威结算。
handoff_outputs:
  - 基线 JSON 与 schema 说明
  - 验证命令和耗时
  - 当前策略差异与证据边界
  - 状态文件更新和唯一下一工作项
```

## 完成记录（2026-09-07）

- 新增 `GameplayObservabilityReport`，以玩家阵营快照、命令校验结果和权威事件生成 `warseed.gameplay_baseline.case.v1` 报告；报告不进入模拟输入。
- 新增五种确定性基线策略：`split_axis`、`concentrated_attack`、`recon_then_commit`、`reserve_policy`、`no_intervention`。策略层拒绝 true-state 或错误阵营快照。
- `grey_ridge_full_match_baseline.gd` 对 3 个锁定敌方计划 × 5 种玩家策略各运行两次，均推进到权威结算或正式 4800 tick 时限，规范化 JSON 与 case fingerprint 重复一致。
- 基线结果为 15/15 `defeat/collapse`。`split_axis` 在 `central_assault` 和 `western_feint` 下坚持到 4800 tick；其余组合在 1983-2477 tick 结算。所有观测到的友方任务 `completed=0`，且部分投入 Supply 的主动策略比无干预更早失败。这些是后续改进所需的工程事实，不是平衡通过或真人乐趣结论。
- 集中进攻在部队存活的两个组合中记录 1 次玩家纠正、60 tick 整卡接管及成功归还；报告同时保留 Supply 分类、任务阻塞、卡牌伤害/承伤/击杀/损失和结算原因。

验证证据：

- 修改前 editor import：PASS；现有 14/14 headless suites：PASS；原 Grey Ridge 6×3 决策矩阵：PASS。
- 修改后 15/15 headless suites：PASS。
- 完整对局矩阵：PASS (`SIMULATED`)，15 cases × 2 repeats；汇总 fingerprint `1f13738901225350cca5dc340f69f09c9c0fee94706156a9d8c66764f9990353`。
- 报告：`artifacts/balance/grey_ridge_full_match_baseline.json`。
- 未运行完整发布门：本工作项未修改权威状态、命令、地图、存档或发布内容。
