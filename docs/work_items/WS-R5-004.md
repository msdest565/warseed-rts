# WS-R5-004：敌方反应审计与合法战后复盘

```yaml
work_item_id: WS-R5-004
phase: R5
status: DONE
blocker_kind: none
depends_on: [WS-R5-003]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DEVELOPMENT_WORKFLOW.md, docs/ENEMY_OPERATION_GUIDE.md, docs/R4_R5_NEXT_AGENT_HANDOFF.md]
objective: 使敌方改令可追溯合法事实、规则、反应延迟、投入成本与执行结果。
in_scope: [typed审计记录, 反应延迟与知识污染测试, 玩家合法战后投影, 双语UI]
out_of_scope: [R6/R7, 性能调优, 真人体验结论, 擅自增加预算]
behavior_contract: 必须区分命令接受、实际执行与目标结果；改变被观察目标须重新累计延迟；过期接触不可变成实时位置；玩家UI不能消费敌方诊断真值或未观察信息。
acceptance_criteria: [隐藏单位/经济/总部污染不改提案, 目标切换重新等待, 成本/事实/规则/结果可追溯, 中英五档UI, 完整发布门]
verification: {focused: tests/tools/enemy_operation_smoke.gd, full_gate: true, performance: DEFERRED_D028, human: OPTIONAL_NOT_RUN}
migration: 审计是本局值拷贝或派生战后证据；若需要持久化，先声明并验证显式版本迁移。
```

此为后续任务契约，尚未开始实现。仅在依赖满足且预算允许时按READY → DISCOVERY → CONTRACT推进；读取最新状态，不能凭本文件跳过当前任务。

完整门：`powershell -ExecutionPolicy Bypass -File tools/verify_grey_ridge_release.ps1 -GodotConsolePath <godot-console>`。灰脊整局入口`tests/scenarios/grey_ridge_full_match_baseline.gd`，默认5策略×3计划×2重复。UI变更使用`tools/run_accessibility_resolution_matrix.ps1`。黄金更新保留失败与反事实/因果差异，不只替换哈希。

READY → DISCOVERY → CONTRACT → IMPLEMENTING：普通反应与护送拦截按观察目标实体切换重置延迟，同一移动目标不每帧重置。审计模块覆盖敌方整卡移动/攻击/战略命令，记录接受、应用与可确认结果；开局、预备、撤退、规则和战略分别标注来源、事实、兵力与承诺成本（移动无Supply收费）。命令队列替换不得记录为已执行。内部审计仅敌方/诊断快照可见；玩家复盘使用另一个仅由当前可见单位产生的typed行为观察，隐藏状态不更新观察结果。可选追加字段兼容旧本局报告，不改存档schema。

验证过程：核心专项包括普通/护送目标切换、同目标移动、旧接触与隐藏单位/总部/经济、应用与队列替换、战略父子任务、部分可见人数、真实射击与玩家合法投影。初次套件出现一次整卡选择失败，独立用例与后续18套未复现，增加失败详情用于定位，未降低断言。首次发布门因审计把下级命令误判为父任务取代而主动中止，二次因英文窄屏弹窗修复中止；这两轮不作为最终门。UI修复涵盖新观察行最小高度与旧失败弹窗英文换行。最终依据release3及后续最终UI日志，不把中途成功片段写成完整门。

Luna完成值类草稿及两次审查，主代理拒绝了不适用的“移动目标每帧重置”和“预备无视野”建议；最终审查请求60秒超时，无自动重试，无测试通过声明。正式代码均由本地验证。新增报告字段可缺省，不替换schema；原存档未变。当前所有证据SIMULATED。

最终验收补充：600tick×三计划审计抽样保留artifacts/r5-004-audit-sample-final.log及JSON，已确认父战略TASK_COMPLETED和实际下级移动分别可追踪。18套重跑PASS，初次孤立选择失败未复现。新观察报告字段使完整对局报告fingerprint自然改变，实际短局黄金18组合保持；必须保留重复确定性与原策略胜负/纠正门。英文失败弹窗自动换行是窄屏复盘后双语路径暴露的真实UI修复。

最终灰脊整局矩阵：15组合各2次、确定性true、quality PASS；分轴与集中各3胜，集中总3次纠正即每局1次，其他策略9败。合法观察在全部15组合中产生66～369条，UI仅显示最近12条。证据artifacts/r5-004-full-match-evidence.json（源完整门release3），不以零纠正替代任务完成或胜利。

release3完整门960.707秒PASS后，英文480宽路径继续暴露已有布局最小宽度：ArmyBoard姿态选项、PrebattlePlanner标题及菜单。仅这两个UI文件有后续变更，实际哈希差异已限定；修复裁切/换行后跑最终五档UI并重新导出/包校验，不重复未改变的权威战斗矩阵。ui4的4档通过不足以声明全5档；以ui5最终结果为准。

VERIFYING → REVIEWING → DONE：最终完整门release3 960.707秒PASS；其后仅两个纯UI文件修复英文窄屏，最终ui5五档真实窗口双语PASS、截图复查、重新Windows导出与包smoke PASS，最终PCK SHA-256 DD5692F388A20D90557A8E27E1228D42E4A62E0AA950CB1DBDF2D50C4FD13578。最终导出/窗口日志无脚本错误。旧选择及鼠标瞬态未在最终完整门/独立UI复现，保留历史证据。未改存档；HUMAN可选NOT_RUN、性能DEFERRED。下一项R5-005 READY。
