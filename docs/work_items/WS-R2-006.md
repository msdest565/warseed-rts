# WS-R2-006 R2 阶段出口

```yaml
work_item_id: WS-R2-006
title: Verify and close the R2 Grey Ridge gameplay benchmark
phase: R2
type: phase_exit
status: DONE_SIMULATED
owner: ai
external_research: optional
objective: >-
  将 R2 已完成的完整对局观测、战场态势、高层意图、异常指挥、因果复盘和策略质量
  纳入可重复阶段门，并验证灰脊在当前正式规模、五档界面和 Windows 导出下保持可运行。
why_now: >-
  WS-R2-001 至 WS-R2-005 已分别完成纵向能力，但完整发布脚本尚未执行新的完整策略质量矩阵；
  在产品负责人审查 R2 残余风险或解锁 R3 前，必须先形成单一可复验的工程出口。
depends_on:
  - WS-R2-001
  - WS-R2-002
  - WS-R2-003
  - WS-R2-004
  - WS-R2-005
source_documents:
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/DECISIONS.md
  - docs/work_items/WS-R2-001.md
  - docs/work_items/WS-R2-002.md
  - docs/work_items/WS-R2-003.md
  - docs/work_items/WS-R2-004.md
  - docs/work_items/WS-R2-005.md
current_evidence:
  - 18 headless suites pass after WS-R2-005
  - the Grey Ridge 5 strategy x 3 plan x 2 repeat quality matrix passes with fingerprint f5afda05a1cb0579c633d61caccf5496b950c32b46d08a0ff19f9c56540c1085
  - prior R2 work items separately passed five-viewport rendering and the shared release gate, but no post-WS-R2-005 phase-exit run exists
  - tools/verify_grey_ridge_release.ps1 does not yet invoke the Grey Ridge full-match quality matrix
in_scope:
  - wire the unfiltered two-repeat Grey Ridge full-match quality matrix into the shared release verification script
  - run editor import, two 18-suite passes, four battle smokes and matrices, the 72-case audit, feedback/tool smokes, Windows export, exported build smoke, and playtest-kit smoke
  - run the 40/80/120 entity benchmark and enforce the existing 80-entity 10 Hz simulation budget
  - run the five-viewport real-render interaction matrix for 1280x720, 1920x1080, 2560x1600, 640x800, and 480x800
  - audit R2 product risks, evidence boundaries, persistence impact, knowledge fairness, localization, and the only legal next queue state
out_of_scope:
  - no R3 card-effect grammar, composition migration, new card content, or gameplay tuning
  - no R4/R5 friendly or enemy AI redesign
  - no new map, campaign state, final asset pass, save migration, release packaging, or Markdown cleanup maintenance
  - no claim that automated evidence proves human comprehension, discoverability, fatigue, or subjective fun
invariants:
  - SimulationWorld remains the authoritative 10 Hz state owner
  - player and Agent actions continue through the same public command validation and queue
  - enemy behavior and baseline strategies consume only legal faction knowledge
  - formal control remains whole-card and snapshots remain value copies
  - the near-term formal scale remains 60-80 active entities; 120 remains an explicitly reported stress measurement
behavior_contract:
  inputs:
    - the frozen R2 implementation and content baseline
    - Godot 4.6.3 stable mono Windows console executable with matching Windows export templates
    - a unique verification session ID; individual test tools isolate their own data without replacing the process-wide Godot template directory
  authoritative_changes:
    - none; this work item only strengthens verification and records stage evidence
  rejection_reasons:
    - any failed import, suite, smoke, matrix, benchmark, tool smoke, export, exported-build smoke, or viewport case blocks the exit
    - a filtered or one-repeat full-match run is diagnostic only and cannot satisfy the release gate
    - any 80-entity P95 above the existing 100 ms simulation tick budget blocks the exit
  events_and_snapshots:
    - existing R2 reports, snapshots, causal events, screenshots, performance lines, and export hashes remain the evidence sources
data_contracts:
  - the full-match report remains warseed.grey_ridge_full_match_baseline.v1
  - the release gate must clear matrix filter environment variables and require all 15 cases with two deterministic repeats
  - no persistence or runtime content schema changes
persistence:
  format_change: false
  migration: none
observability:
  - release output names every gate and reports elapsed time
  - full-match artifact records quality status, determinism, strategy profiles, failures, and aggregate fingerprint
  - viewport artifact records all five resolutions and inspected screens
  - performance output reports 40, 80, and 120 entity simulation and presentation P95 values
acceptance_criteria:
  - the shared release verification invokes the default unfiltered full-match quality matrix and fails if it is incomplete, nondeterministic, or below quality expectations
  - all 18 headless suites pass twice, including the isolated-session pass
  - all four battle smokes and per-battle matrices plus the 72-case audit pass
  - the Grey Ridge full-match report contains 15 cases, two repeats per case, determinism_passed=true, quality_audit.status=PASS, and no failures
  - the 80-entity simulation P95 remains within the 10 Hz budget; 120 is reported without becoming a formal content target
  - all five real-render viewport cases pass R2 battlefield overlay, command desk, exception, after-action, localization, focus, scroll, and non-overlap checks
  - Windows debug export produces EXE and PCK artifacts and the exported executable passes isolated headless startup
  - no save format changes occur and R2 evidence is explicitly labeled SIMULATED
  - product risk review distinguishes proven engineering behavior from unproven human experience and leaves R3 blocked until product-owner acceptance
verification:
  baseline:
    - git status --short
    - git diff --check
    - inspect WS-R2-005 full-match report and completion evidence
  focused:
    - tests/scenarios/grey_ridge_full_match_baseline.gd with no filters and two repeats
    - tools/run_accessibility_resolution_matrix.ps1
    - artifacts/balance/grey_ridge_full_match_baseline.json schema and quality audit
    - artifacts/accessibility_resolution_matrix.json five-resolution audit
  full_gate: true
  optional_research:
    - developer comparison of split-axis, concentrated attack, and reserve-policy playthroughs
expected_files:
  - tools/verify_grey_ridge_release.ps1
  - artifacts/balance/grey_ridge_full_match_baseline.json
  - artifacts/accessibility_resolution_matrix.json
  - build/windows/warseed-debug.exe
  - build/windows/warseed-debug.pck
  - docs/work_items/WS-R2-006.md
  - docs/AI_DEVELOPMENT_STATE.md
risks:
  - the release gate becomes several minutes slower because it now includes 30 complete deterministic matches
  - overriding process-wide APPDATA hides Godot export templates; the full gate must run with the host Godot data directory and rely on its session isolation arguments
  - fixed scripted routes can establish reachable differentiated strategies but cannot prove player discoverability or fun
  - real-render viewport automation depends on a Windows desktop session and remains distinct from the headless release script
  - current commander tasks are persistent orders rather than finite phased graphs; task completion quality remains an R4 responsibility
rollback: >-
  Remove the full-match invocation from the release script and restore the prior R2 queue state. No save, content,
  command, simulation, or player-data migration is involved.
handoff_outputs:
  - full release-gate transcript summary and duration
  - full-match fingerprint and strategy quality profile
  - five-viewport artifact and visual audit summary
  - 80/120 entity performance evidence and Windows export hashes
  - product risk review and explicit product-owner decision request
```

## 冻结说明（2026-09-07）

R2 工程出口证明的是一套可重复运行、合法、确定且具有策略差异的灰脊玩法基准。它不把脚本代理胜率解释为真人乐趣，也不自行接受 R2 产品出口。完整工程门通过后，R2 应进入 `COMPLETE_SIMULATED_AWAITING_PRODUCT_ACCEPTANCE`；只有产品负责人接受残余风险后才能把 R3-001 解锁为 `READY`。

## 完成记录（2026-09-07）

- 统一发布脚本已永久接入默认无筛选、双重复的灰脊完整策略质量矩阵；调用前清除三个诊断筛选环境变量，调用后恢复原值，避免局部诊断运行假冒完整门。
- UI 实渲审查发现命令回执横幅遮挡顶部资源与战况文字；横幅已改为地图内 440px 居中布局，480px 宽视口保留 20px 边距。五档脚本新增“回执在地图内且不与资源栏、战况栏、态势开关相交”的硬断言。
- 最终连续完整发布门通过，耗时 `1426.714` 秒：editor import、两轮 18/18 suites、Legacy 与四关 smoke、各关矩阵、72/72 发布审计、30 场完整质量矩阵、40/80/120 实体、反馈与工具 smoke、Windows 导出、导出包启动和试玩包 smoke 全部通过。
- 最终性能模拟 P95 为 40/80/120 实体 `8.059/13.373/18.197 ms`；80 实体低于 10 Hz 的 100ms 权威预算，120 只保留为扩展测量。
- 五档真实窗口矩阵在 Intel Arc/OpenGL 下通过：`1280x720`、`1920x1080`、`2560x1600`、`640x800`、`480x800`；每档覆盖战役选择、战前编成、战场态势、高层意图、异常队列、暂停、战后三区和反馈。
- 完整策略报告保持 `15` 案例、每案 `2` 次重复、`determinism_passed=true`、`quality_audit.status=PASS`、零 failures，fingerprint 为 `f5afda05a1cb0579c633d61caccf5496b950c32b46d08a0ff19f9c56540c1085`。
- Windows 导出 EXE SHA-256 为 `679DF06F7F9F2D2293768747AA9AD71FB996869249960179ADA8180445FC0A7A`；PCK SHA-256 为 `0C652652EC713A337261F8137A0E7654E7A0E93A3EFC33517323363174676567`。
- 首次完整门使用隔离 `APPDATA` 时因找不到导出模板失败；正常用户目录在受限会话中又不可写。最终门使用工作区隔离目录加临时只读模板联接完整通过，联接随后删除，真实模板保持不变。只有最终连续成功运行计入出口证据。
- 本阶段未改变存档格式。知识公平、统一命令管线、整卡操作和值拷贝边界保持通过。所有证据均为 `SIMULATED`，不证明真人理解、发现性、疲劳或主观乐趣。

## 产品风险审查

- 已证明：灰脊至少存在两种兵力投入、路线、Supply 与速度/保存权衡不同的可胜策略；三类主动或无干预劣势策略稳定失败；结果可追溯且跨三个锁定敌方计划确定。
- 尚未证明：真人能否自然发现总部突击转换、是否愿意使用高层意图与异常队列、重复对局疲劳和主观乐趣。D-026 允许这些保持可选研究，但不能在对外描述中声称已经验证。
- 已控制：固定路线/时点过拟合由三计划、两种成功策略、三类失败策略、双重复和完整因果门约束；不能外推到其他地图。
- 后续必需：R3 解决卡牌战术动词与同质化；R4 解决持久任务 `completed=0`、阶段化任务图和重规划；R5 再扩展敌方行动 AI。未经产品负责人接受本审查，R3-001 保持 `BLOCKED`。
