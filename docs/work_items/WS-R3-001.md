# WS-R3-001：类型化战法语法与加载验证

```yaml
work_item_id: WS-R3-001
title: Validate a bounded typed doctrine effect grammar at content load
phase: R3
type: data_contract
status: DONE
owner: ai
external_research: optional
objective: 七类 typed Resource 战术语法在内容加载时拒绝无效配置，并保持旧十二战法兼容。
why_now: 维护 002 已通过完整自检，用户已授权继续 R3，当前战法仍只有文本和名称分支。
depends_on: [WS-R2-006, WS-MAINT-20260910-002]
source_documents:
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/R3_TACTICAL_GRAMMAR.md
  - docs/EXPANSION_SYSTEMS_AND_CONTENT_SPEC.md
current_evidence:
  - 维护最终完整门 1884.207 秒 PASS，两轮 18/18，运行源码未变，作为本项基线。
  - DoctrineDefinition 仅有四个名称/文本字段；BattleDefinition 只收集战法 ID 与引用。
in_scope:
  - DoctrineEffectDefinition 及 Trigger/Selector/Effect/Cost/Timing/Counterplay/Reason 子资源。
  - 首条 STAGED_DEPARTURE 语法、稳定 ID、受限枚举、完整组合、字段路径错误。
  - BattleDefinition 与 Loader 接入校验；typed .tres fixture 与损坏内容回归。
out_of_scope:
  - 不迁移正式战法行为；效果执行器在 R3-002 单独完成。
  - 不实现 composition、v4 存档、新战斗维度、卡牌与 R4/R5 AI。
invariants:
  - 不允许内容参数字典、脚本表达式或执行路径。
  - 不修改权威 tick、命令、状态、快照与存档。
behavior_contract:
  inputs: [静态战法 Resource 及七类必需子资源]
  authoritative_changes: [none]
  rejection_reasons:
    - NULL_REFERENCE：空效果/子资源。
    - EMPTY_ID 或 INVALID_VALUE：空/非法稳定 ID、未知语法版本或枚举、时序超界。
    - DUPLICATE_ID：单战法效果 ID 重复；同种效果重复也拒绝以避免隐式叠加。
    - EMPTY_DISPLAY_NAME 或 INVALID_VALUE：说明 key 空或不符合本地化 key 语法。
  events_and_snapshots: [none；本项仅产生 DataValidationResult 字段路径]
data_contracts:
  - schema_version=1；首条只支持将领任务建立、已部署下属卡、分批出发及部署顺序时序。
  - 基础延迟 0..3600 tick、卡间隔 1..3600 tick，全部整数。
  - 成本为后续部队延迟投入，反制为分批抵达时的局部兵力空窗，退出为玩家接管/任务结束。
  - 效果 ID 使用小写字母开头及小写字母/数字/下划线，最长 64；本地化 key 使用大写字母/数字/下划线。
  - 旧 effects=[] 合法；七子资源缺失不能用默认值悄悄补齐。
persistence:
  format_change: false
  migration: none
observability: [结构化 reason 与完整字段路径, 校验不修改源 Resource]
acceptance_criteria:
  - 有效 typed .tres 加载及存储往返成功，七子资源类型保持。
  - 空项、重复、未知枚举、非法时序、缺说明逐项拒绝，错误能定位战法与字段。
  - 内容加载器拒绝非法战法，四关与旧十二战法仍有效。
  - 两种语言解释 key 可解析，全部 18 套回归通过。
verification:
  baseline: [维护最终完整门的两轮 18/18 与四关加载]
  focused: [tests/unit/test_doctrine_effect_definitions.gd, tests/unit/test_data_resources.gd]
  full_gate: false
  optional_research: [none；无玩家交互变化]
expected_files:
  - src/data/doctrine_*_definition.gd
  - src/data/battle_definition.gd
  - tests/unit/test_doctrine_effect_definitions.gd
  - tests/unit/test_data_resources.gd
  - tests/fixtures/doctrine_staged_departure.tres
  - locale/zh_CN.po
  - locale/en.po
risks: [将 schema 有效误解为执行器已完成, Resource 共享引用污染, 旧内容兼容]
rollback: 移除新增语法校验即可回到旧静态战法契约，无存档迁移。
handoff_outputs: [状态与契约更新, focused/回归日志, 唯一下一项 WS-R3-002]
```

领取记录：READY → DISCOVERY → CONTRACT。已复核源文档、加载链和旧交替掩护分支；基线来自紧邻且源码哈希相同的维护完整发布门，不重复运行无变化基线。D-021/D-022 不属于本项依赖，仍待用户决定。技术文档的字典提案已由 D-027 受限 typed Resource 要求取代。

执行：CONTRACT → IMPLEMENTING → VERIFYING → REWORK → VERIFYING。首次专项检测到无效 fixture 污染当前进程缓存：Godot `duplicate(true)` 没有深复制 catalog 所引用的全部外部资源。测试构造改为 `duplicate_deep(Resource.DEEP_DUPLICATE_ALL)`，保留缓存隔离断言；校验器本身不写 Resource，正式内容文件没有被修改。解析导入通过，正在重跑专项。

验收：VERIFYING → REVIEWING → DONE。`artifacts/r3-001-import.log` 解析通过；`artifacts/r3-001-focused-final.log` 为 `R3_GRAMMAR_FOCUSED failures=0`；`artifacts/r3-001-tests.log` 18/18 PASS、退出码 0，无脚本/测试错误。实际 diff 与字段错误路径已复核，`git diff --check` 通过。旧十二战法和四关 catalog 加载保持兼容。新反制 key 中英均可解析，未修改任何正式卡牌动作、命令或存档，因此按数据契约验证矩阵未重跑 UI/性能/完整发布门；维护 002 的完整门仍作为紧邻工程基线。全部证据 `SIMULATED`，本项无真人研究要求。

下一项 `WS-R3-002`：迁移交替掩护的实际任务时序与原因到 kind 注册器；当前语法有效不能被解释为执行器已接线。D-021/D-022 继续待答复，完整 goal 尚未完成。
