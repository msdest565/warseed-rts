# WS-R5-001：敌方准则与分阶段行动计划

```yaml
work_item_id: WS-R5-001
phase: R5
status: DONE
depends_on: [WS-R4-006]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DESIGN_AND_DECISION_TREES.md, docs/TECHNICAL_PLAN.md, docs/R4_R5_NEXT_AGENT_HANDOFF.md]
objective: 将既有敌方开局和后续行动编译为开局前冻结的typed计划，明确阶段、目标、兵力上限与撤退条件。
in_scope: [typed准则与阶段, 兼容编译, 独立世界所有系统, 阶段执行与快照, 公平性与确定性测试]
out_of_scope: [R5-002预备策略, R5-003新模板, R5-004复盘UI, R6/R7, 性能调优]
behavior_contract: 开局在读取玩家本局行为之前冻结；普通命令校验/应用；只用己方状态、合法观察与预先公开地图目标；已撤退行动不得被后续进攻抢回。
acceptance_criteria: [非法ID与范围拒绝, 隐藏状态污染不改变开局与锁定目标, 实际阶段与撤退执行, 旧快照不变, 四关内容兼容, 完整发布门]
verification: {focused: tests/tools/enemy_operation_smoke.gd, full_gate: true, performance: DEFERRED_D028, human: OPTIONAL_NOT_RUN}
migration: 旧BattleEnemyPlanDefinition字段继续支持，由集中适配器编译typed阶段；不改存档格式，不改稳定计划ID。
```

2026-09-16 READY → DISCOVERY → CONTRACT → IMPLEMENTING。基线ab94ea7已推送。既有三种开局和固定后续仍保留语义；现有后续直接读取玩家总部enabled/当前位置，改为开局冻结的地图目标区，战斗接敌仍须合法可见。阶段系统从SimulationWorld抽离，世界只负责所有权与调度。新阶段/准则资源可覆盖兼容编译结果，不能通过自由字典或脚本表达式定义行为。


实现：新增EnemyOperationDoctrine/PhaseDefinition/Definition、世界所有EnemyOperationSystem及值拷贝快照。旧四关资源由单一编译器兼容，不读取玩家本局行为选择开局；公开地图目标区在开局冻结，删除后续进攻对隐藏玩家总部真值的依赖。普通命令验证与执行、真实到达阶段完成、实际兵力投入上限、减员至阈值或以下时锁存撤退均已接通；战略、反应和拦截不覆盖撤退。玩家常规快照不含敌方计划。

专项与18套回归已通过（r5-001-focused-final.log、r5-001-regression.log），完整发布门r5-001-release.log进行中；未提前DONE。Luna短契约审查r5-001-contract-review建议明确严格小于/包含相等阈值，实际契约选择“至阈值或以下”，与R4保守撤退一致；没有战中快照恢复格式，不虚构恢复测试。初次专项暴露测试字段destination误写，修正为target_position；含脚本错误的focused.log作失败保留，不能引用为通过。


首轮完整门因灰脊300tick黄金漂移退出1，保留r5-001-release.log。诊断脚本r5-001-drift-counterfactual.gd仅临时关闭新撤退并还原旧佯动日志格式，18个组合全部恢复原黄金，证明迁移差异来自新撤退准则和阶段命令记录，而非未解释的战斗漂移。正式实现保留新行为，更新对应16个黄金并给18组合增加二次运行确定性比较；r5-001-golden-verified.log全部通过。根据Luna意见新增包含等号的阈值边界专项，通过r5-001-boundary.log。默认doctrine额外承诺时间下限为0，原计划commitment_ticks仍保留，不无故延迟旧关卡。

最终冻结r5-001-frozen-final.json，完整门r5-001-release-final.log进行中。Luna为R5-002生成的策略资源草稿留在artifacts/delegation/r5-002-policy-draft，待001 DONE后才可接入；尚未实施002。


第二轮门在整局矩阵发现集中进攻超时：诊断r5-001-concentrated-probe.log显示双方主力并非阵亡，我方停在接近点且总部尚未进入视野，旧代理依赖敌军接战把部队拉近。修复仅增加“已到接近点但仍未发现总部”时一次向前侦察，全部使用玩家合法快照，并计入纠正额度，不降低胜负门槛、不关闭敌军撤退。单组合诊断r5-001-concentrated-fixed.log在369tick获胜，11条命令接受、0拒绝、1次纠正；此单例不代替全矩阵。最终门改为r5-001-release-final2.log，冻结r5-001-frozen-final2.json；前一轮已停止，不能记录为完整通过。

最终VERIFYING → REVIEWING → DONE：r5-001-release-final2.log完整发布门765.543秒退出0，18套、四关完整矩阵、存档、导出/包校验通过；719受验文件无漂移。PCK SHA256 `61E65FC2F34A74D86C1C68A5EF6A90E6A1ECC2DEB9FA31002304A5892987414D`。UI本项未改布局，复用R4五档实渲并通过本轮反馈五档集成。HUMAN可选未运行，性能D-028暂缓。下一项R5-002。
