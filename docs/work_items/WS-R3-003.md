# WS-R3-003：稳定混成编制与安全军团存档

```yaml
work_item_id: WS-R3-003
title: Preserve mixed-card composition through deployment, losses and safe roster migration
phase: R3
type: composition_persistence_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 一张两兵种整卡在部署、接管、补员、战损、撤出及跨关恢复中保持稳定条目身份，旧档安全迁至 v4。
why_now: R3-002 已完成，用户明确接受 D-021；维护 003 完成后按队列领取。
depends_on: [WS-R3-002, D-021]
source_documents:
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/DECISIONS.md
  - docs/R3_ENTRY_REVIEW.md
  - docs/R3_COMPOSITION_AND_SAVE_COMPATIBILITY.md
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/EXPANSION_SYSTEMS_AND_CONTENT_SPEC.md
  - docs/EXPANSION_IMPLEMENTATION_ROADMAP.md
current_evidence:
  - UnitCardDefinition 目前只有单个 unit_definition_id 与 authorized_strength。
  - 开局创建、预备部署与战地补员读取单一兵种；战损和撤离只保留整卡总数。
  - ArmyRosterStore v3 直接覆盖文件，损坏加载返回空字典，宿主无法区别缺档与坏档。
in_scope:
  - typed composition entry、集中旧卡适配、引用/重复 ID/数量/排序校验。
  - 条目成员身份、授权/可用/当前/撤出兵力与值拷贝快照；正式控制粒度保持整卡。
  - 开局与预备部署、战地补员、撤离、战损写回和编成页补充的条目闭环。
  - v1/v2/v3 保守迁至 v4 main，迁移来源与内容版本可审计。
  - 完整内存校验、备份、临时文件与原子替换，结构化加载/保存失败及可见 UI。
  - 一张使用现有兵种的两条目 fixture；旧十二卡不重编。
out_of_scope:
  - D-022 的新传感器、压制、专业弹药、伤害标签与新战术动词。
  - 正式十二卡混成比例调整，灰脊内容迁移和后续阶段实现。
  - 目录搬迁、既有存档槽重排、自动删除备份或自动丢弃未知卡牌。
invariants:
  - SimulationWorld 10 Hz 权威；统一命令/校验；Agent 仅合法阵营知识。
  - entry ID 永久稳定，不按数组下标识别，不把混成兵种降格成任意字典内容契约。
  - 旧档迁移不赠兵、不重编，荣誉/装备/组织/累计损失和战役计数保留。
  - 失败不覆盖原档，不用空档继续保存；旧快照不随新世界变化。
behavior_contract:
  inputs: [typed composition Resources, v1/v2/v3/v4 军团 JSON, 既有整卡命令和补员操作]
  authoritative_changes:
    - 卡牌编制按稳定 entry ID 绑定成员，兵种创建和补充消费真实条目缺口。
    - 补充按 replacement_priority 升序、entry ID 升序填补；顺序不取决于 Resource 数组排列。
    - 每条目的幸存者独立恢复和撤出，整卡总数等于条目总数；操作只选整卡。
    - 工程支援能力依据实际存活工程成员，UI 和统一验证消费相同事实。
  rejection_reasons:
    - 空/重复 ID、未知兵种、非正授权数量、非法优先级及总编制冲突拒绝加载。
    - 坏 JSON、错误字段类型、负数/溢出、未来版本、条目不匹配显式失败。
    - 备份、临时写入或原子替换失败均保留原档并返回可显示的原因。
  events_and_snapshots:
    - 条目快照提供成员身份及兵力，旧控制/部署/撤离事件继续可审计。
    - UI 显示混成明细和失败原因，失败不声称存档已保存或恢复。
data_contracts:
  - UnitCardCompositionEntry 使用 entry_id、unit_definition_id、authorized_count、formation_role、replacement_priority。
  - 旧单一兵种定义由集中兼容编译生成 main，不污染共享 Resource。
  - 现有 role 与工程动作继续沿用，不借编制切片引入新战斗规则。
persistence:
  format_change: true
  migration: v1/v2/v3 -> v4；旧兵力写入 composition.main，保存迁移源版本与独立内容版本。
observability: [条目兵力明细, 备份位置, 明确加载/保存失败, 条目身份回归证据]
acceptance_criteria:
  - 一张两兵种卡完整通过开局/预备部署、接管/归还、补员、战损、撤出、保存/恢复。
  - 条目顺序调整不会交换兵种或伤亡；补员填入正确缺口；空编制旧卡保持黄金行为。
  - v1/v2/v3 无损迁移、v4 往返、损坏/未来版本/不匹配/写入失败保留原档通过。
  - 宿主禁止坏档变空军团覆盖，界面给出明确错误，四关连续隔离恢复通过。
  - 18 套及新增专项、五档实渲、80 实体、完整策略矩阵、完整发布门通过。
verification:
  baseline: [维护 003 完整门完成后核对基线]
  focused: [composition 数据验证, 混成全流程, 安全迁移负例, 宿主/UI 失败路径, 四关链式存档]
  full_gate: true
  optional_research: [混成明细与存档错误的真人理解]
expected_files: [src/data/, src/simulation/, src/persistence/, src/app/simulation_host.gd, src/ui/, locale/, tests/]
risks: [旧单兵种假设遗漏, 阵亡实体清理后丢条目身份, 未知卡牌记录丢失, Windows 原子替换失败]
rollback: 保留原始迁移备份；旧程序不能读取 v4 时使用备份，不尝试猜测或逆向重编。
handoff_outputs: [正常状态与迁移契约, 完整验证和构建证据, 唯一下一项 WS-R3-004 仍要求 D-022]
```

维护 003 完整门期间只做只读调查和契约准备，没有提前修改混成运行时。维护现已 DONE，基线完整门 `1487.349s` PASS、323 文件哈希一致。WS-R3-003 按 READY → DISCOVERY → CONTRACT 领取：已核对开局、预备、补员、撤离、编成补充和存档六条路径及工程能力消费者，冻结以上契约与 `R3_COMPOSITION_AND_SAVE_COMPATIBILITY.md`。D-021 的决定已满足，下一步 IMPLEMENTING。

CONTRACT → IMPLEMENTING。已加入 typed entry 与集中旧卡编译、成员身份与快照，接通初始/预备部署、按优先级补员、撤离和条目恢复；工程能力消费实际存活成员。军团 v4 引入明确的加载结果、完整内存校验、临时写入/备份/原子替换及故障返回；宿主坏档阻止开局，编成购买先保存后提交，失败提示与重试入口可见。

IMPLEMENTING → VERIFYING → REWORK → VERIFYING（2026-09-11）。补齐正式悬停说明、统一命令部署/补员/撤出、错误字段、购买回滚及战后重试不重复奖励后，专项 r3-003-focused7.log PASS，最终18套 r3-003-tests-final.log PASS。连续五档先后在战后标签和历史入口的物理鼠标同步处失败，返工统一定位并激活测试窗口后 r3-003-ui-full3.log 五档全部 PASS；原悬停、焦点、点击和遮挡断言均保留。

每档新增混成战前明细、延迟卡牌说明、坏档禁开局、保存失败提示与真实点击重试。四关链式工具显式点击当前战后“部队卡”页后操作成长/补员，并同步真实鼠标；r3-003-campaign.log 完成四关、16人补充、rescue_citation 跨关继承，正常退出0。564个受验源码哈希、五档JSON、链式JSON及桌面/窄屏截图归档 artifacts/r3-003-evidence/。

VERIFYING → REVIEWING → DONE：完整发布门 r3-003-release.log 已通过，正常退出0，用时1745.289秒。两轮18套、Legacy与四关、72案例、15×2完整对局、性能、反馈工具、Windows导出及包内隔离启动、试玩包校验全部PASS。564个受验源码哈希前后一致，实际diff及git diff --check通过；证据均为SIMULATED。

完整策略汇总指纹保持 a994185d975a06e046fe608567e9adb685b6a9525a83fdc85b858477d8a51255；80实体模拟/表现P95为15.125/1.678ms。PCK为6209388 bytes，SHA-256为48CF8410D841ABB68CC3E92B565D1DDED80EDD9BED1BEF3F74CBD9F151B772EB。摘要 artifacts/r3-003-summary.json；全部证据归档 artifacts/r3-003-evidence/，详见正常状态文档第16节。真人研究OPTIONAL_NOT_RUN；下一项WS-R3-004仅等待D-022产品决定，完整R3/R4/R5目标尚未完成。
