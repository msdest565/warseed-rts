# WS-MAINT-20260916-002：方案反馈、工程渡口与战后段决策修复

```yaml
work_item_id: WS-MAINT-20260916-002
phase: R5
status: DONE
objective: 修复重新生成缺少反馈、工程标记与阻挡不一致，以及全据点占领后的侦察决策缺口。
depends_on: [WS-R5-004]
source_documents: [docs/AI_DEVELOPMENT_WORKFLOW.md, docs/work_items/WS-R4-002.md, docs/work_items/WS-R4-003.md, docs/CONTENT_AND_ONBOARDING_DEVELOPMENT_PLAN.md]
in_scope: [方案刷新反馈与具体失败原因, 工程路线阻挡与操作入口, 合法迷雾侦察决策和已发现总部进攻, 回归验证和可玩包]
out_of_scope: [R6-R7, 性能优化, 存档格式, 隐藏敌方坐标, 随机更换相同输入的方案]
behavior_contract:
  - 每次重新生成提供可见回执；同输入同方案明确说明；失败说明缺少目标、可用兵力或不同方案。
  - 工程路线标记范围开通前全部是权威导航障碍；两种既有合法开通命令都清除同一范围，视觉同步。
  - 工程筛选在工程卡尚未部署时提供部署入口，操作结果仍通过权威回执确认。
  - 全部可占领据点属于本方且未知总部时，提供侦察卡继续探索公开迷雾边界的决策；使用既有持续侦察任务和统一命令。
  - 已合法发现敌方总部时，显示总部进攻决策；重要后续决策不能被支援筛选掩盖。
  - 不读隐藏建筑位置、不改写快照或从UI直接写权威状态。
acceptance_criteria: [方案按钮实窗验证, 工程路径开通前后与两类命令回归, 据点到侦察到总部决策链与知识污染测试, 完整发布门, 五档中英实窗, 导出包校验]
verification: [tests/test_runner.gd, tools/verify_grey_ridge_release.ps1, tools/run_accessibility_resolution_matrix.ps1]
migration: none
external_research: optional
```

2026-09-16 READY → DISCOVERY → CONTRACT → IMPLEMENTING。初始工作区干净。发现灰脊路线标记为16×8格，但原始测试地图仅部分格子阻挡；方案窗暂停后重复计算没有新反馈，失败原因丢失；全部据点已占领后缺少后续行动入口。性能遵循D-028；证据仅SIMULATED。

修复发现与证据：左侧工程入口仍筛选已移除的旧支援类型，而灰脊实际使用OPEN_ROUTE技能；现映射到真实技能并保留未部署工程卡入口。灰脊标记原范围覆盖东部补给点，已收窄为障碍自身(140,60,8,4)，全范围初始化阻挡。AStar revision刷新现在同时清除移除的solid格。首次完整门因过宽范围阻挡地区目标返回REWORK；修复范围后专项focused6及18套suites2全部通过，无脚本错误。侦察目标从合法迷雾与公开地形选择可达边界，不使用敌方总部坐标。显式继续侦察/进攻决定原子更新姿态，避免HOLD让指令不前进。

Luna只读审查调用maint-002-plan-review在60秒超时，未采纳未返回内容；无自动重试。主代理完成代码与证据审查。

VERIFYING：真实五档中英矩阵maint-002-ui3.log通过，包括鼠标重复生成、可见相同结果回执、修改后重算、左侧工程入口→技能实际完成→地图清除、筛选下持续侦察入口→真实任务。输入试验暴露强制SpinBox.apply会恢复旧文本，已移除该多余处理，采用原有焦点提交机制。截图检查1280及480通过；工程完成事件补齐区域/位置后窄屏正确显示。最终完整门前专项focused-final通过，源码哈希冻结于maint-002-frozen.json。

附加SIMULATED证据：maint-002-natural-recon.gd在全部据点已持有且移除敌军干扰的fixture中，只提交一次正常侦察决策；未传送侦察兵、未揭开迷雾，tick1098首次合法观察到总部。证明持续探索能推进，不是实战必胜或在敌军拦截下必定到达的承诺。正常敌军策略由完整发布矩阵覆盖。

2026-09-16 VERIFYING → REVIEWING → DONE：最终完整发布门maint-002-release2.log PASS，973.265秒；两次18套、四关smoke/决策矩阵、灰脊15组合×2完整对局、平衡审计、反馈/存档/导出及包smoke全部通过。无SCRIPT ERROR/ERROR；日志中的playtest gate FAIL为评估器负例fixture，对应工具退出0并验证拒绝路径。五档真实窗口maint-002-ui3.log PASS，未新增性能要求。实际diff和冻结765文件哈希一致；git diff --check通过。

交付：`build/playtest-kits/WARSEED-R5-Fix-20260916.zip`，37,797,124字节；SHA256 `157E561CFD980793F052F730766F2C978EA764A1C6D622EDA103B08ED36D798F`。PCK SHA256 `52A013ECF2265CBFC4B693149A3C76E18F5C7A62A02F1CE34DD56476A36BBCE2`。ZIP CRC、17份manifest文件大小/哈希/压缩内容全部通过（maint-002-package-check.txt）。解压后运行START_WARSEED_PLAYTEST.cmd。

操作说明：重新生成会明确显示次数、相同结果或失败原因；它仍按相同局势确定性计算。开通工程路线入口显示工程技能或待部署工程卡；灰脊技能需工程部队在280距离内、安全静止准备3秒，移动/接敌会中断，界面说明既有费用规则。全据点持有后选择继续侦察，侦察按合法迷雾推进；发现总部后选择进攻，显式交回AI并采用相应姿态。无存活侦察卡时需先部署/补充侦察力量。

风险与下一步：真人主观体验仍NOT_RUN且可选；有敌军拦截时侦察可规避或受阻，不承诺无条件找到总部；性能按D-028未验收。当前维护结束，下一项none，R6/R7未开始。累计主模型保守估算约836.08美元（以本地token日志计、Luna排除），低于1000美元授权上限；非实际账单。
