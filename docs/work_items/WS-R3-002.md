# WS-R3-002：交替掩护的类型化执行器

```yaml
work_item_id: WS-R3-002
title: Execute staged departure through an effect kind registry
phase: R3
type: authoritative_equivalence_migration
status: DONE
owner: ai
external_research: optional
objective: 交替掩护从战法 ID 分支迁移为类型化效果与确定性任务参数，保持原行为。
why_now: R3-001 语法与加载验证已通过，需要证明一条效果端到端执行。
depends_on: [WS-R3-001]
source_documents:
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/R3_TACTICAL_GRAMMAR.md
  - docs/work_items/WS-R3-001.md
current_evidence:
  - R3-001 18/18 PASS，语法专项 PASS。
  - artifacts/r3-002-golden-before.json 保存旧行为 61 tick 多卡、接管、归还、改令轨迹。
in_scope:
  - kind 注册器只消费合法阵营快照与 typed 战法定义，返回值拷贝任务参数。
  - 正式 alternating_cover Resource 接入 STAGED_DEPARTURE，移除世界类三处名称分支。
  - TaskState/TaskSnapshot 保留效果 ID、触发 tick、等待 reason 和退出条件。
  - 将领等待原因、装备原因及悬停解释消费数据，保持原核心事件与战斗轨迹。
out_of_scope:
  - 不迁移其余十一战法、不新增战斗动词/单位、不重编卡或迁移存档。
  - 不实现阶段化任务图、参谋 COA 或敌方新计划。
invariants:
  - SimulationWorld 应用参数且继续 10 Hz，Agent/玩家都经统一命令队列。
  - 注册器拒绝真值快照、错误阵营和不合法卡；不读取隐藏敌方任务/单位。
  - 旧快照和执行结果不持有可变 Resource 引用。
behavior_contract:
  inputs: [任务建立时的合法阵营快照, 将领 ID, 卡 ID, 已装备 typed 战法定义]
  authoritative_changes:
    - activation_tick = 当前 tick + base_delay + 已部署下属卡稳定序号乘 interval。
    - 正式交替掩护 base=0、interval=20；被接管卡仍占排序位置，但其任务不重建。
    - 时序适用时优先于旧火力准备等兼容分支，原任务系统继续处理等待/取消/归还。
  rejection_reasons: [TRUE_STATE_FORBIDDEN, WRONG_OBSERVER_FACTION, FACTION_KNOWLEDGE_REQUIRED, INVALID_EFFECT_DEFINITION, COMMANDER_OR_CARD_REQUIRED, CARD_NOT_ELIGIBLE]
  events_and_snapshots:
    - 任务效果元数据按值复制，等待原因由实际已应用效果提供。
    - 原事件内容与 gameplay fingerprint 保持；不把诊断元数据加入已有因果 hash。
data_contracts:
  - 配置由 kind 分发；改变战法 definition_id 不改变动作，更改 timing 则改变 activation。
  - 多效果参数冲突按固定顺序处理；当前一张战法只允许一个分批出发动作。
persistence:
  format_change: false
  migration: none
observability: [任务参数与触发/退出元数据, 原将领原因文本, 战法代价与反制说明]
acceptance_criteria:
  - 旧 61 tick 黄金轨迹一致；完整 15x2 策略指纹一致。
  - 换战法 ID、改 timing、玩家接管/归还、同 tick 重下令、空效果兼容均通过。
  - 隐藏敌情污染不改变结果，真值/错误观察者被拒绝，旧任务快照不跟随新值。
  - 18 套回归、四关、五档实渲、80 实体和完整发布门通过。
verification:
  baseline: [artifacts/r3-001-tests.log, artifacts/r3-002-golden-before.json]
  focused: [注册器纯函数测试, 命令任务集成, 黄金轨迹]
  full_gate: true
  optional_research: [反制文字理解仍为可选研究]
expected_files:
  - src/doctrines/
  - src/simulation/core/simulation_world.gd
  - src/simulation/entities/task_state.gd
  - src/simulation/core/task_snapshot.gd
  - data/army/alternating_cover.tres
  - data/army/effects/staged_departure.tres
  - tests/unit/test_doctrine_effect_runtime.gd
risks: [初始任务建立时阵营快照未完整, 共享资源缓存, 等价迁移改变排序或优先级]
rollback: 恢复本项分支与数据增量即可，无存档变化。
handoff_outputs: [黄金/完整矩阵证据, 完整门与 UI, WS-R3-003 决策依赖状态]
```

READY → DISCOVERY → CONTRACT。原语法回归已通过；迁移前黄金脚本经真实旧路径正常退出 0，覆盖两张同将领部队及玩家临时接管、归还和多次目标变更。正式修改前冻结此轨迹，后续不得为通过测试修改期望。

CONTRACT → IMPLEMENTING → VERIFYING。正式 Resource 已接入效果，注册器只读取合法阵营快照和静态定义，权威世界应用返回值。三处交替掩护名称分支已移除；仅保留静态旧资源目录映射。新 `DoctrineTaskParameters` 记录已应用效果、触发 tick、等待原因、成本、反制和退出条件，任务快照逐字段值拷贝。编成页说明追加配置中的反制文本。

黄金轨迹前后 SHA-256 均为 `c4dd3d05dce912c94d39b9ad5a27cc44ddb95a028e7b5f66647350e2920fecda`；该轨迹已纳入永久测试。`artifacts/r3-002-focused.log` 为 `R3_EFFECT_FOCUSED failures=0`，覆盖命令改名/改时序、同 tick 改令、接管/归还、未知配置、真值/错误观察者拒绝、敌情污染和旧快照不变。导入无解析错误。18 套回归和五档实渲正在运行；之后仍须完整发布门。

18/18 回归 PASS (`artifacts/r3-002-tests.log`)。实渲首跑前两档通过，在 2560×1600 的既有决策历史点击失败；测试窗口高于物理屏幕，历史按钮位于屏幕外。VERIFYING → REWORK → VERIFYING：该步骤显式将窗口右下区域移入屏幕、定位真实鼠标，并新增 hover 必须命中按钮的断言后再点击。未修改运行时 UI 或放宽原点击/历史断言；完整发布门尚未启动。

超高专项通过；随后五档在最窄战后页签 hover 断言失败（实际悬停仍落在 RowsScroll），仅发送合成 MouseMotion 会被实际鼠标位置覆盖。页签验证现在先把目标定位到物理屏幕、warp 真实鼠标，再检查原 hover 与键盘 focus；480×800 专项通过 (`artifacts/r3-002-ui-narrow.log`)。这两次返工只修改测试定位，运行时代码与黄金期望均未变；最终连续五档为 `artifacts/r3-002-ui-final2.log`。

最终五档连续实渲 PASS、退出码 0，无脚本或测试错误。结果与反制说明截图已归档至 `artifacts/r3-002-evidence/`。完整发布门正在同一进程持续运行，日志 `artifacts/r3-002-release.log`；两轮 18/18、Legacy 与灰脊 smoke、灰脊开局矩阵已通过，尚不能将整个发布门记为 PASS。运行前源码清单为 `artifacts/r3-002-evidence/source-manifest.json`，运行期间只补充文档。

预审已核对注册器知识边界、固定排序、值拷贝、公开命令集成和实际 diff；未发现需返工的实现问题。独立查看编成截图确认反制文本可见。其余十一战法继续兼容，存档格式不变。D-021/D-022 保持 Deferred；即使本项发布门通过，也须先满足 D-021 才能领取 WS-R3-003。

最终验收：VERIFYING → REVIEWING → DONE。完整发布进程正常退出 0，`1930.230s` PASS；两轮 18/18、Legacy/四关 smoke 与矩阵、72 案例、15×2 完整对局、反馈/工具、Windows 导出/隔离启动和试玩包校验全部通过。日志无脚本/解析/测试错误。完整策略质量与确定性 PASS，指纹仍为 `a994185d975a06e046fe608567e9adb685b6a9525a83fdc85b858477d8a51255`，满足等价迁移验收。

80 实体模拟/表现 P95 `13.582/1.726 ms`，正式门通过；120 实体 `22.077/2.626 ms`，仅压力证据。导出 EXE SHA-256 `679DF06F7F9F2D2293768747AA9AD71FB996869249960179ADA8180445FC0A7A`；PCK `6167268` bytes / SHA-256 `FF7966887AEFC3D872256F10A4AF7DFE435E8331EA3603827D7C2B93E888E472`。完整门期间 321 个受验源码文件未改变；最终汇总 `artifacts/r3-002-summary.json`，独立矩阵、UI、截图和源码清单在 `artifacts/r3-002-evidence/`。

最终 diff、文档链接与 `git diff --check` 通过。无存档变化，无提交、推送、分支或 PR。所有工程和视觉证据均为 `SIMULATED`；真人反制说明理解与主观体验仍为可选 `NOT_RUN`。下一项 R3-003 因 D-021 尚未接受而 BLOCKED；等待之前已发送方案的用户答复，R3/R4/R5 完整 goal 继续保留为未完成。
