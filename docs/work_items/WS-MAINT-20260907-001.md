# WS-MAINT-20260907-001：Markdown 权威性与引用清理

```yaml
work_item_id: WS-MAINT-20260907-001
title: Markdown authority and reference cleanup
phase: maintenance
type: documentation_audit
status: DONE_SIMULATED
owner: ai
blocker: none
objective: >-
  删除无唯一证据价值且已被新状态系统取代的 Markdown，合并重复交接说明，并为仍需保留的历史文档增加明确的归档边界。
in_scope:
  - 全仓 Markdown 入链、出链、脚本打包引用和状态权威声明审计
  - 删除无引用且内容已由 AI_DEVELOPMENT_STATE、GAMEPLAY_REWORK_ROADMAP 或决策日志覆盖的文档
  - 修正 README、工具脚本和文档索引中的失效链接
  - 对仍被测试包、历史证据或迁移设计引用的文档标记 ARCHIVED/OPTIONAL_RESEARCH
out_of_scope:
  - 不删除构建证据、原始测试记录、决策日志或仍被工具打包的协议
  - 不改变游戏行为、阶段顺序或产品决策
current_audit_findings:
  - docs/AI_HANDOFF.md 错把 STATUS_AND_NEXT_STEPS.md 声明为权威状态，仍写 13 套测试和旧下一优先级；仅由 README 链接，属于首个删除/合并候选
  - PLAYTEST_PROTOCOL.md 与 P6_7_PLAYTEST_COHORT_PLAN.md 仍被试玩包构建和 smoke 测试硬引用，当前不能直接删除
  - EXPANSION_IMPLEMENTATION_ROADMAP.md 仍被主工作流和新路线图作为历史架构参考引用，应归档标记而非删除
  - AI_GOAL_COMMANDS.md 的推荐顺序仍指向已完成的 WS-R2-005，AI_DEVELOPMENT_WORKFLOW.md 仍把 WS-R2-001 写成当前唯一入口
acceptance_criteria:
  - 所有删除候选均有引用审计记录和替代权威来源
  - rg 不再发现旧权威声明、旧阶段领取指令或失效链接
  - 工具 smoke 与 git diff --check 通过
```

## 完成记录（2026-09-07）

- 删除 `docs/AI_HANDOFF.md`：该文件仅由 README 入链，错误地把历史 `STATUS_AND_NEXT_STEPS.md` 声明为实时权威，并重复保存 13 套测试和旧下一优先级；替代入口为 `AI_DEVELOPMENT_STATE.md`、`AI_DEVELOPMENT_WORKFLOW.md` 与 `AI_GOAL_COMMANDS.md`。
- README 将实时状态源、工作流、目标协议、玩法路线和决策日志列为当前权威层；旧 P1-P6 长文明确降级为历史实现摘要，并同步当前 18 套测试、R2 出口与完整策略矩阵口径。
- `CURRENT_PROJECT_ASSESSMENT.md` 标为 `ARCHIVED`；`AI_DESIGN_AND_DECISION_TREES.md` 限定为 R4/R5 设计参考；`EXPANSION_IMPLEMENTATION_ROADMAP.md`、`STATUS_AND_NEXT_STEPS.md` 与可选真人研究协议因仍承载历史、架构或打包价值而保留。
- 修正 `AI_GOAL_COMMANDS.md` 和 `AI_DEVELOPMENT_WORKFLOW.md` 的旧领取入口，当前唯一阶段入口统一为 `/goal review phase-exit R2`。
- 43 份源码 Markdown 的相对链接审计为零失效，代码围栏审计为零异常；`tests/tools/playtest_kit_smoke.ps1` 通过，`git diff --check` 通过。
- 本维护项不改变运行时、存档、性能、公平知识、本地化或阶段决策；全部证据为 `SIMULATED`。
