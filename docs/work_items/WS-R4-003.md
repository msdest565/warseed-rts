# WS-R4-003：方案比较与玩家确认

```yaml
work_item_id: WS-R4-003
title: Compare, revise, approve and reject staff plans
phase: R4
type: ui_command_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 玩家可从正常决策区比较方案、修改目标/预算/风险/可用卡，明确批准或拒绝并获得权威确认。
why_now: R4-002已生成方案，但没有玩家入口或权威批准记录。
depends_on: [WS-R4-002]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DEVELOPMENT_WORKFLOW.md, docs/work_items/WS-R4-002.md, docs/AI_DESIGN_AND_DECISION_TREES.md]
current_evidence: [983b87a已推送, R4-002专项/18套/四关通过, 原将领命令会重派全体下属而不支持方案逐卡预备]
in_scope: [正常决策区入口, 双语方案比较弹层, 请求修改和拒绝, 统一命令批准及重新校验, 值拷贝权威方案/回执, 五档实窗与完整门]
out_of_scope: [阶段任务图及计划实际调度属于R4-004, 自动撤退/重规划属于R4-005, 敌方AI, 跨局存档]
invariants: [UI不写世界, PLAYER审批经同一校验/队列, 不抢手控卡, 不超预算, 合法快照, 不将方案已批准写成战场已执行]
behavior_contract:
  inputs: typed请求、profile_id、完整方案指纹和command_id；生成/展示只读
  authoritative_changes: 队列应用时重新生成并校验方案；保存本阵营最新批准方案和结构化回执，发布值拷贝快照；不下达伪造的全将领指令
  rejection_reasons: INVALID_TARGET、STAFF_PLAN_STALE、TASK_CONFLICT、AGENT_NOT_AUTHORIZED、BATTLE_CONCLUDED；无副作用失败
  events_and_snapshots: STAFF_PLAN_DECIDED事件与StaffPlanDecisionSnapshot（状态/方案/命令/tick/reason）；私有批准记录按阵营过滤
data_contracts:
  - 方案选择无默认自动批准；评分不宣称最优或胜率，显示投入/预备、部署Supply、准备时长、风险与逐卡路线。
  - 修改请求后丢弃旧候选，显式重新生成；拒绝关闭当前建议且不影响既有批准/控制/资源。
  - 打开比较时战术暂停，关窗恢复打开前状态；批准入队显示等待，恢复推进后按快照显示已批准或拒绝，不将入队视为已执行。
  - 冻结的完整方案指纹防止战况变更和载荷篡改；提交和应用两次验证，重复待处理批准拒绝。
  - 批准只存本局方案，R4-004消费该真实批准对象建立任务图；003不能声称任务图已实现。
  - 队列持有请求值拷贝，旧快照/回执/批准对象不可被UI后续修改污染。
persistence: {format_change: false, migration: none}
observability: [权威审批事件, 宿主命令回执, 快照计划状态, UI排队/批准/失败文本]
acceptance_criteria:
  - 中英文及五档窗口均能从决策区打开、比较2-3方案、修改条件、拒绝和批准，键盘与鼠标可达。
  - 批准仅经统一命令管线写本阵营方案；输入过期/篡改/重复/越权及同tick前序变更均明确拒绝，失败不覆盖旧批准。
  - 暂停时只入队，恢复后权威回执；观察者隔离、值拷贝、重开清空成立。
  - 拒绝/关闭不提交命令、不动部队；原18套、四关、完整策略、80性能、五档和Windows完整发布门通过。
verification:
  baseline: [git status --short, git diff --check, R4-002最终回归PASS]
  focused: [tests/tools/staff_plan_approval_smoke.gd, tests/test_runner.gd, 五档中英实窗]
  full_gate: true
  optional_research: [真人方案理解与纠正负担仍可选NOT_RUN]
expected_files: [src/ui/staff_plan_panel.gd, src/ui/command_desk.gd, src/commands/staff_plan_approval_command.gd, src/simulation/systems/staff_plan_system.gd, src/simulation/core/world_snapshot.gd, src/simulation/core/simulation_world.gd, src/app/simulation_host.gd, locale, tests]
risks: [阶段执行尚待004, 频繁战况变化要求重新生成, 评分为启发式]
rollback: 审批无跨局数据，移除入口和审批状态即可恢复旧流程；不得影响已有存档。
handoff_outputs: [验证日志, 状态更新, 下一项WS-R4-004]
```

2026-09-13 READY → DISCOVERY → CONTRACT → IMPLEMENTING：已核查现有命令和UI。将审批与执行分开按依赖实施，不用将领全卡指令替代具体方案，也不把003的批准记录当成004完成。

2026-09-13 IMPLEMENTING → VERIFYING → REWORK：复核发现五档测试曾在鼠标点击后直接触发按钮回调，不能据此证明弹窗鼠标可达。移除回调补偿并要求每次真实输入路径恰好触发一次；2560x1600首次复测失败，正在修正测试的嵌入窗口坐标路由。同时补上审批入口的本地玩家阵营校验，防止伪造PLAYER身份审批敌方方案，并增加无队列/无审批副作用回归。旧发布门结果未可靠确认，须在修复后重跑；本项尚未DONE。

REWORK → VERIFYING：审批专项 `artifacts/r4-003-focused-final.log` PASS，覆盖真实同tick接管先于审批应用的队列竞态。五档中英矩阵 `artifacts/r4-003-ui-final.log` 全部退出0；弹窗鼠标走根窗口输入路由，无直接回调补偿，生成/拒绝/批准均恰好触发一次。桌面截图已检查。源码哈希清单 `artifacts/r4-003-source-hashes.json` 已冻结，最终完整发布门 `artifacts/r4-003-release-final.log` 正在执行，尚未宣称通过。

VERIFYING → REWORK：窄屏批准截图暴露语言刷新把隐藏字段标签重新显示，三列紧凑网格因此错配标签与选项。已修复刷新及onready初始化顺序，并新增双语标签可见性断言。旧门在两轮18/18、灰脊30场完整对局及其他关卡部分矩阵通过后主动停止，留档 `artifacts/r4-003-release-before-layout.log`，不作为最终完整门。重新运行五档 `artifacts/r4-003-ui-layout-final.log` 后冻结最终源码，再跑完整门。

REWORK → VERIFYING：修正布局后的双语五档全部PASS，480x800批准截图确认紧凑字段、决策按钮与六卡总览均可用。649文件哈希已重新冻结；最终门为 `artifacts/r4-003-release-final2.log`，退出码独立记录在 `artifacts/r4-003-release-exit2.txt`。此前旧门仅作历史定位证据。

2026-09-13 VERIFYING → REVIEWING → DONE：最终完整门 `artifacts/r4-003-release-final2.log` 665.333秒PASS、退出0，无脚本错误；两轮18/18、Legacy、四关smoke/矩阵、72案例、15×2完整对局、反馈工具、Windows导出与包校验全部通过。完整策略指纹仍为 `4fb412b52bca4e5aee0f659694f5b7e9ab086c1b570571d5890f329b2030855f`。80实体模拟/表现更新P95为5.712/0.744ms，120实体8.785/1.207ms仅压力测量，不把headless表现更新当作实渲帧时间。

专项与最终双语五档全部PASS，鼠标生成/拒绝/审批各恰好一次，按钮键盘焦点可达；修改失效、战术暂停/恢复确认、敌方观察隔离、请求/队列/快照值拷贝、越权/伪造/过期/重复拒绝和真实同tick接管竞态均通过。桌面与480窄屏截图已检查。649个受验文件哈希变化0，实际diff及git diff --check通过。

PCK为6364044 bytes，SHA-256 `40C2A740CDAE10BF8B592D78829D2F98283D3623F1F3AEFD0EBC2051E963A9E2`；证据归档 `artifacts/r4-003-evidence/`，摘要 `artifacts/r4-003-summary.json`。所有证据为SIMULATED；真人对方案理解、发现性和纠正负担仍为可选NOT_RUN。本项没有跨局存档变化，也没有完成阶段执行或R4出口。唯一下一项WS-R4-004：消费批准方案，建立真实逐卡阶段任务图。
