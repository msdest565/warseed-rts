# WS-MAINT-20260914-001：手动战斗可靠性与可玩包

```yaml
work_item_id: WS-MAINT-20260914-001
title: Restore visible and responsive manual combat and deliver a playable build
phase: R4
type: maintenance
status: DONE
owner: ai
external_research: optional
objective: 修复多单位消失、手控自卫/Q进攻/移动、已发现总部决策，解释并修复异常减速，交付Windows游戏包后暂停开发。
why_now: 2026-09-14用户最新明确要求优先修复并出包，随后暂停。
depends_on: []
source_documents: [docs/AI_DEVELOPMENT_WORKFLOW.md, docs/TECHNICAL_PLAN.md, docs/AI_DESIGN_AND_DECISION_TREES.md]
current_evidence: [R4-004在验收中且18套与五档通过，完整门尚未运行, 用户报告六项手控战斗问题]
in_scope: [批量表现切换, 手控武器反应, Q攻击移动与普通移动, 合法已发现敌方总部决策, 减速诊断, 回归/五档/60-80性能/发布门, Windows可玩包]
out_of_scope: [R4-005及后续阶段, 新内容系统, 存档格式改动, 改变专业武器弹药与识别设计]
invariants: [10Hz权威模拟, 玩家整卡操作, 敌情来自合法快照, 命令校验一致, 保留R4-004现有工作]
behavior_contract:
  inputs: 玩家选卡/移动/Q攻击移动；射程内可见敌情；已发现指挥所位置。
  authoritative_changes: 手控卡保有局部自主武器反应且不被旧任务限制，普通移动不被自主追击打断；攻击移动可靠接敌并继续；发现总部后可明确下发进攻地点决策。
  rejection_reasons: 保留真实不可达/不可见/专业武器约束，不因旧Agent任务或低组织锁住玩家基本命令。
  events_and_snapshots: 既有命令回执、射击/命中事件、单位/卡快照、决策reason；表现层不改变权威位置。
data_contracts: [稳定卡ID与现有typed决策, 不引入自由脚本]
persistence: {format_change: false, migration: none}
observability: [切换前后单位可见与位置, 手控攻击/位移与原因, 总部发现前后决策, 减速来源]
acceptance_criteria: [六项报告各有定位与验证, 实渲不消失且手控可交战/移动, 隐藏总部不泄露, 完整发布门和Windows包启动通过, 交付后暂停]
verification:
  baseline: [R4-004回归18/18 PASS, 五档PASS, git diff --check]
  focused: [手控战斗专项, 表现阈值专项, 真实窗口交互, 60/80实渲]
  full_gate: true
  optional_research: [HUMAN主观体验可选NOT_RUN]
expected_files: [src/presentation, src/simulation, src/input, tests, docs, locale]
risks: [手动反应改变完整策略轨迹, 批量渲染需保留性能, 未完成R4阶段不能标阶段出口]
rollback: 保留原存档与R4工作；产物标为当前维护试玩包，不宣称R4/R5已完成。
handoff_outputs: [修复与验证记录, 可直接运行Windows包, 提交推送, 暂停状态]
```

READY → DISCOVERY → CONTRACT → IMPLEMENTING：按最新用户指示中断后续阶段，保留R4-004验收中代码；先定位表现阈值、手控任务门禁、攻击移动、总部投影与编队速度。

2026-09-14用户补充：出包前增加补给数值显示。沿用同一维护项，左侧支援区顶部固定展示合法阵营快照的当前补给/上限，支援列表滚动与语言切换不能隐藏或保留旧数值；随五档UI和发布门验证。

2026-09-14产品负责人明确授权：本次可玩包暂时放宽帧数要求，停止继续优化帧数并优先出包。保留实际60/80实渲数据和原失败日志，不能将其改写为16.667ms门已通过；此例外仅用于当前维护包，不自动修改后续阶段性能标准。其余权威回归、合法知识、存档和Windows启动检查继续执行。

发布门首轮在灰脊 `b_split_armor/western_feint` 的旧黄金指纹停止。`artifacts/manual-matrix-audit.log` 单变量对照证实：禁用新增手控反应时恰好恢复旧 `22a1c4...`，装甲自主反应0次；启用后两次均为 `368fa11...`，装甲自主反应4次。这是用户明确要求的第2项行为变化，更新该唯一黄金指纹，其余17条保持不变。不是降低策略质量或确定性门槛。

2026-09-14 VERIFYING → REVIEWING → DONE：完整发布门 `artifacts/manual-release-final2.log` 727.735秒PASS，正常退出0且无脚本错误；两轮18/18、四关smoke/矩阵、72案例、30场完整对局、反馈工具、Windows导出与包校验全部通过。`manual-battle-final2.log` 手控专项和四地图总部路线、`manual-visibility2.log` 55/57/80/40实体实渲、`manual-shipping-ui.log` 中英五档均PASS。保留用户授权的实渲帧数例外：80实体帧P95 16.803ms，不声称原16.667ms门通过。

最终包 `build/playable/WARSEED-Playable-20260914-final.zip`，SHA-256 `1AF08EEC4AA0A80AB181C6C0FB46103A5B34C91C2B8E483BC0F9237EBDC738C8`；运行同名目录的 `WARSEED.exe`。PCK与最终发布门导出完全一致，独立解压校验三文件及包内真实窗口选关/编成/战场/补给4/10/空格暂停均通过。635项游戏内容与先前包一致，唯一导出差异是Godot UID缓存，采用最终导出组包。712个保留受验文件哈希一致，移除未使用临时启动脚本及UID；实际diff和空白检查通过。所有证据SIMULATED，HUMAN可选NOT_RUN，无存档迁移。

完整归档与风险见 `AI_DEVELOPMENT_STATE.md` 第20节。按用户要求提交推送当前相关工作后暂停开发，保留R4-004实现但不标阶段任务DONE，不继续R4-005或R5。
