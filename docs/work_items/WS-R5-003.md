# WS-R5-003：多轴、牵制、欺骗与补给切断模板

```yaml
work_item_id: WS-R5-003
phase: R5
status: BLOCKED
blocker_kind: BUDGET_RESERVE
depends_on: [WS-R5-002]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DEVELOPMENT_WORKFLOW.md, docs/ENEMY_OPERATION_GUIDE.md, docs/R4_R5_NEXT_AGENT_HANDOFF.md]
objective: 通过typed内容让三种敌方计划产生实际不同的进攻压力与补给后果。
in_scope: [typed阶段与三套正式模板, 轴线/投入/目标差异, 地区补给争夺因果, 必要对局修复]
out_of_scope: [R6/R7, 性能调优, 真人体验结论, 擅自增加预算]
behavior_contract: 三种计划不能只改延迟；锁定目标来自公开地图或合法知识；补给切断必须由普通控制/结算系统实际减少对手地区收入；不得新增免费部队或改变既定质量门。
acceptance_criteria: [三模板可观察差异, 地区收入切断与恢复对照, 三计划乘多策略自然结束, 重复确定性, 完整发布门]
verification: {focused: tests/tools/enemy_operation_smoke.gd, full_gate: true, performance: DEFERRED_D028, human: OPTIONAL_NOT_RUN}
migration: 新资源稳定ID与引用由Loader校验，旧四关继续兼容；不改玩家存档格式。
```

此为后续任务契约，尚未开始实现。仅在依赖满足且预算允许时按READY → DISCOVERY → CONTRACT推进；读取最新状态，不能凭本文件跳过当前任务。

完整门：`powershell -ExecutionPolicy Bypass -File tools/verify_grey_ridge_release.ps1 -GodotConsolePath <godot-console>`。灰脊整局入口`tests/scenarios/grey_ridge_full_match_baseline.gd`，默认5策略×3计划×2重复。UI变更使用`tools/run_accessibility_resolution_matrix.ps1`。黄金更新保留失败与反事实/因果差异，不只替换哈希。

R5-002现已DONE，工程依赖满足；主模型已进入370美元验收预留线，400美元总额不重置。暂停新增实现，不把本项或整个R5写成DONE。
