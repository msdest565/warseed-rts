# WS-MAINT-20260916-001：低成本开发与代理交接规范

```yaml
work_item_id: WS-MAINT-20260916-001
title: Establish bounded low-cost delegation and evidence-based acceptance
phase: R4
type: development_workflow
status: DONE
owner: ai
external_research: optional
objective: 让后续代理以小上下文领取任务、按风险分工并提交可独立核验的结果，减少重复阅读、无效重试和重复验收。
why_now: 用户2026-09-16要求降低token成本，授权使用指定低成本服务。
depends_on: []
source_documents: [AGENTS.md, docs/AI_DEVELOPMENT_STATE.md, docs/AI_DEVELOPMENT_WORKFLOW.md, docs/AI_GOAL_COMMANDS.md, docs/GAMEPLAY_REWORK_ROADMAP.md]
current_evidence: [238613a已推送, 工作区预检干净, R4_USER_PAUSE, 测试入口当前18套, 指定服务模型发现成功]
in_scope: [渐进式阅读入口, 风险分工与升级规则, 委派及验收模板, 后续阶段接手路径, 受限外部文本协作工具, 文档和工具验证]
out_of_scope: [恢复游戏开发, 修改玩法或发布包, 放宽既有工程门, 全自动外部模型执行shell, 购买或充值服务]
invariants: [状态文件唯一实时源, 用户暂停有效, 密钥不入库不输出, 外部返回不自动执行, 单一主要工作项]
behavior_contract:
  inputs: 当前任务契约、允许读取/修改清单、验收命令；外部服务显式模型和精简任务文本。
  authoritative_changes: none
  rejection_reasons: 越界修改、预算或上下文超限、缺失证据、外部服务错误均停止该次委派并记录。
  events_and_snapshots: none
data_contracts: [委派包带base_commit和文件哈希, 结果带证据与usage, 模型名不视为能力证明]
persistence: {format_change: false, migration: none}
observability: [请求次数、usage、耗时、拒收原因、最终验收来源]
acceptance_criteria: [新代理可找到唯一当前入口, 简单与高风险任务分工明确, 验收复用与失效规则明确, 密钥安全和失败处理可验证, 外部服务能力如实记录]
verification:
  baseline: [git status --short, git diff --check]
  focused: [文档链接和围栏检查, 工具离线测试, 一次真实小任务调用]
  full_gate: false
  optional_research: [HUMAN主观体验不适用]
expected_files: [AGENTS.md, .gitignore, docs/AI_*, docs/work_items/WS-MAINT-20260916-001.md, tools/ask_low_cost_ai.py, tests/tools/test_low_cost_ai.py]
risks: [第三方价格和模型路由可变, 低成本模型返工可能抵消节约, 既有记录包含过时阶段指令]
rollback: 仅开发工具与规范，不改变游戏运行；外部服务不可用时使用本地代理或只交接文档。
handoff_outputs: [分工手册, 委派模板, 服务接入记录, 正常状态更新]
```

READY → DISCOVERY → CONTRACT：核对现有协议、暂停状态、真实测试入口与服务公开入口。此次授权用于开发流程维护，不视为恢复R4/R5或自动接受新产品决策。

CONTRACT → IMPLEMENTING → VERIFYING：新增低成本分工、任务模板、代码导航、恢复顺序与证据复用规则；修正入口的历史阶段指令和14套旧口径。新增无工具权限、无自动重试、密钥不回显的一次文本调用工具。

用户追加指定四个模型，实施同题同限制各一次比较：Luna六项判断全对、零误报，27.605秒，总1895token；Kimi/Agents无可用正文，DeepSeek本次超时。选择Luna为当前L0/L1试用首选，失败不计为模型能力零分，不宣称综合最强或量化省钱。详细限制与回执路径见 `AI_LOW_COST_PROVIDER.md`。没有额外付费重试，也未把网关返回的代码自动执行。

VERIFYING → REVIEWING → DONE：`python tests/tools/test_low_cost_ai.py` 5/5离线检查通过，覆盖输入/输出限制、无工具请求、重定向拒绝、正文密钥脱敏、失败无重试、重复run-id拒绝、空响应拒绝。检查11个改动文件、20条本地Markdown链接、围栏闭合和凭据字面值泄漏，错误0；实际diff及 `git diff --check`通过。四次真实调用记录为SIMULATED，失败和费用UNKNOWN均保留，未运行游戏发布门（仅开发规范/独立Python工具变化，游戏源码未改），HUMAN研究不适用。

当前阶段仍R4_USER_PAUSE，下一项R4-004保持BLOCKED；新代理经正常AGENTS入口进入手册，外部模型本地路径配置位于忽略目录，不随Git泄露。交付当前工作区文档与工具，本次未提交或推送，不改变09-14可玩包。
