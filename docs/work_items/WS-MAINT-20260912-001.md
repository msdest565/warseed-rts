# WS-MAINT-20260912-001：增援入口共享冷却

```yaml
work_item_id: WS-MAINT-20260912-001
phase: R3
type: ui
status: BLOCKED
objective: 决策区增援后所有补员入口同步置灰和倒计时，冷却到期可再次打开决策列表。
depends_on: [WS-MAINT-20260911-001]
source_documents: [docs/AI_DEVELOPMENT_STATE.md, docs/AI_DEVELOPMENT_WORKFLOW.md, docs/GAME_DESIGN_DOCUMENT.md]
in_scope: [左侧上下文入口, 决策动作倒计时, 两卡补员集成测试, 五档实窗验证]
out_of_scope: [改变权威费用或冷却, 新增兵员, 存档迁移, R3战术贡献]
behavior_contract:
  inputs: 本阵营快照的共享支援冷却终点与当前tick
  authoritative_changes: none
  interaction: 冷却中左侧入口与同类决策动作均禁用；到期后左侧可打开列表，不依赖当前选择，逐卡动作仍保留各自合法性校验。
  display: 所有入口剩余tick向上取整为秒；暂停冻结；满编卡可按已有规则移除补员行。
acceptance_criteria:
  - 补充一张卡后另一张减员卡的补员动作同步置灰与倒计时。
  - 最后不足一秒显示1秒，到期后同时恢复，无选中卡也可打开列表。
  - 冷却期间点击不新增命令；双语和五档实窗通过。
verification:
  regression: tests/test_runner.gd
  ui: tests/tools/accessibility_resolution_matrix.gd
  full_gate: false
persistence: 无变化
external_research: optional
```

READY → DISCOVERY → CONTRACT → IMPLEMENTING → VERIFYING：复核发现上一轮保留整个按钮禁用状态会将当前选择错误应用到阵营决策入口；现仅共享冷却控制上下文入口，具体动作继续依据逐卡快照校验。18/18回归通过，日志 `artifacts/maintenance-reinforcement-cooldown-final-tests.log`，覆盖实际命令、另一张减员卡、暂停与到期；五档正在验证。证据均为 SIMULATED。此维护先完成，R3-005保持REWORK。

实窗环境阻塞：`artifacts/maintenance-reinforcement-cooldown-ui-rework.log` 中真实补员、左右禁用和倒计时断言通过，但鼠标横坐标为316663616，悬停提示检查失败。桌面执行申请被自动审批服务503拒绝，未绕过，五档未通过。修正灰脊迁移后的四将领/六卡旧断言；维护保持BLOCKED，R3-005可继续不依赖桌面的实现与纯模拟专项。此状态不代表修复已完成五档验收。
