# WS-MAINT-20260910-002：战术暂停中的规划与信息披露

```yaml
work_item_id: WS-MAINT-20260910-002
title: Plan and issue orders while battlefield time is paused
phase: maintenance
type: ui_input_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 让玩家在战术暂停中读取部队状态、理解选项并完成决策和路线规划。
why_now: 用户确认决策区不足、主攻轴含义重复、选项无说明且缺少可操作的暂停。
depends_on: [WS-MAINT-20260910-001]
source_documents:
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/GAME_DESIGN_DOCUMENT.md
  - docs/work_items/WS-MAINT-20260909-002.md
current_evidence:
  - 基线 18/18 PASS，artifacts/maint-002-baseline.log
  - 目标决定终点；不同的主攻轴区域实际是途经点，相同则直达。
  - Esc 暂停 SceneTree，无法操作 HUD；现有 tooltip 只延迟一秒，没有进度。
in_scope:
  - 右侧纵向扩大意图和决策区；底部居中按将领分组的紧凑部队卡。
  - 每张卡显示现员/编制，将领汇总兵力、风格和绿/黄/红加短文本的任务状态。
  - 区分最终目标与可选途经区，直达不重复选择目标。
  - 姿态、风格、预备队的逐项说明，等待 0.5 秒后显示 0.5 秒进度，随后显示文本。
  - 空格战术暂停，仅冻结宿主推进，菜单、规划、统一命令提交继续工作。
out_of_scope:
  - R3-R5 实现另按其稳定工作项依次执行，不混入本维护项。
  - 不改变兵种、数值、存档、经济和现有预备队规则。
invariants:
  - SimulationWorld 10 Hz 权威、同一命令验证队列、合法阵营知识和值拷贝快照。
  - 不用颜色替代文本，不以隐藏敌方状态判定遇敌。
behavior_contract:
  inputs:
    - 空格按下仅切换一次，键盘重复和松开不触发按钮；战前/结算/菜单中不切换。
    - 暂停中可提交意图、姿态、部署、支援与路线；收到排队回执，恢复后按原队列执行。
  authoritative_changes:
    - 暂停期间 tick、移动、战斗、经济、冷却和 Agent 不推进；不积累暂停时间，不补跑。
    - 重开和开始新会战清除战术暂停；Esc 菜单关闭后保留先前战术暂停。
  rejection_reasons: [沿用统一验证结果，不把等待恢复写成无响应失败]
  events_and_snapshots: [不新增权威字段；暂停是宿主调度状态，响应窗仍按 tick]
data_contracts:
  - 直达在 UI 使用空途经 ID，提交时规范化为目标 ID，兼容既有意图契约。
  - 悬停说明须对应当前真实行为；未接入自动消费的预备策略须明确仍需玩家投入。
  - 状态红色优先于黄色，合法快照中的交战/附近可见敌人优先；无任务则绿色。
persistence:
  format_change: false
  migration: none
observability: [暂停常驻文字、队列回执、本局历史、任务颜色与文本、逐项说明]
acceptance_criteria:
  - 五档窗口内地图、右侧决策和底部军团无覆盖，窄屏提供纵向滚动，部队与汇总均可达。
  - 空格暂停和恢复不跳 tick；暂停下操作 UI、绘制路线并提交命令可用且恢复后生效。
  - 每项姿态/风格/预备候选均有等待、进度和说明；移出/换项/关闭重置。
  - 原补员、预备投入、具名失败、固定卡 ID、历史确认无回归。
verification:
  baseline: [18 suites PASS]
  focused: [战术暂停确定性与排队测试, tooltip 时间边界, 部队汇总/状态测试, 真实空格与选项交互]
  full_gate: true
  optional_research: [主观布局、说明理解和暂停操作体验]
expected_files:
  - src/app/simulation_host.gd
  - src/app/game_root.gd
  - src/input/camera_controller.gd
  - src/ui/army_board.gd
  - src/ui/command_desk.gd
  - src/ui/hover_tooltip.gd
  - scenes/game/game_root.tscn
  - locale/zh_CN.po
  - locale/en.po
  - tests/integration/test_game_integration.gd
  - tests/tools/accessibility_resolution_matrix.gd
risks: [窄屏空间不足, 原生下拉提示与自定义提示冲突, 同 tick 多条意图的确认]
rollback: 恢复本项宿主调度与界面增量，无存档迁移。
handoff_outputs: [正常开发状态记录, 验证证据, 依次推进 R3/R4/R5 的阶段审查]
```

执行：READY → DISCOVERY → CONTRACT → IMPLEMENTING。用户明确底部每张部队卡显示兵力，由将领汇总并显示风格和简短任务；空闲绿、日常任务黄、交战或遇敌红。该新需求替代此前只显示将领的 HUD 布局限制。完整活动目标仍包括维护自检后依次完成 R3、R4、R5；本项不提前变更阶段。

验证进度：IMPLEMENTING → VERIFYING → REWORK → VERIFYING。focused 集成通过（`artifacts/maint-002-focused6.log`），覆盖暂停分数 tick 保持/无补跑/重开、命令队列、同批意图替代历史、隐藏知识状态色及 tooltip 0.5 秒边界。最窄实渲 PASS（`artifacts/maint-002-ui9.log`）；桌面前两档通过，完整五档仍在验证。修复窄屏回执覆盖、动态下拉重建、提示层鼠标穿透与窄屏镜头适配。Godot PopupMenu 忽略 velocity 为零的模拟鼠标运动，测试事件补齐速度；超宽窗口右侧移到物理屏幕内进行持续悬停。所有证据 SIMULATED。

补充审查：首次发布门在完成两轮 18 套及早期矩阵后主动中止，不计为完整 PASS（`artifacts/maint-002-release.log`）。原因是补查底部姿态菜单时发现 tooltip 在屏幕边缘回夹后被 PopupMenu 遮挡；现按菜单矩形选择上方/下方空位，进度条同样避让。扩展后的五档矩阵 `artifacts/maint-002-ui-final2.log` 全部 PASS，并保存 `tactical_bottom_posture_help.png` 与 `tactical_route_planning.png`；最窄底部提示及桌面暂停路线截图视觉抽查通过。最终 focused `artifacts/maint-002-focused-final2.log` PASS。最终完整发布门正在写入 `artifacts/maint-002-release-final.log`，必须等待正常退出后才能判定。

R3 执行前的产品决定方案已单独整理到 `docs/R3_ENTRY_REVIEW.md`，仅作后续设计准备，不改变本项实现范围或阶段。D-021/D-022 仍待用户答复；当前维护验证不受影响。

最终门中间结果：两轮 18/18 与灰脊完整 15 案例 × 2 复跑 PASS，完整矩阵耗时 1007.4 秒，零 failures、确定性与质量门均通过。aggregate fingerprint 保持 `a994185d975a06e046fe608567e9adb685b6a9525a83fdc85b858477d8a51255`，与 001 完成时一致。独立证据副本位于 `artifacts/maint-002-evidence/`（五档交互 JSON、完整对局 JSON、最窄底部提示、桌面暂停规划截图和 306 个源码/内容/验证文件哈希）。完整发布门仍未结束，不能据此提前标记 DONE。

## 最终验收（2026-09-10）

VERIFYING → REVIEWING → DONE。最终连续完整发布门正常退出 0，耗时 `1884.207s`。两轮 18/18、Legacy/四关 smoke、各关矩阵、72 案例审计、30 场完整对局、反馈/工具验证、Windows 导出、隔离启动和试玩包 smoke 全部通过。日志没有脚本/解析/测试错误；测试工具内部的 FAIL/INCOMPLETE 为预设负例，其 smoke 本身通过。

80 实体模拟/表现 P95 为 `15.462/1.812 ms`，正式 10 Hz 门通过；120 实体为 `19.315/2.391 ms`，仅作扩展压力测量。五档真实输入与截图矩阵已由 `maint-002-ui-final2.log` 独立通过。EXE SHA-256：`679DF06F7F9F2D2293768747AA9AD71FB996869249960179ADA8180445FC0A7A`；PCK `6147024` bytes，SHA-256：`B686ADC446726320AFEBA71B7C9241D93D36BBB69D56FDB07B1611BF58EAA3D4`。摘要为 `artifacts/maint-002-summary.json`。

实际 diff 与 306 文件哈希已审查，`git diff --check` 通过。无存档迁移，知识和统一命令边界保持。全部自动化及视觉抽查为 `SIMULATED`；布局理解和主观操作体验为可选 `NOT_RUN (HUMAN)`。旧英文复盘重复翻译 key 未扩大清理。没有提交、推送、分支、PR 或新正式候选包。

用户已明确授权维护自检后顺序完成 R3、R4、R5，此授权作为接受既有 R2 工程出口并继续的依据；此前披露的固定路线过拟合、真人发现性和主观乐趣未知仍保留。唯一下一项为 `WS-R3-001`，D-021/D-022 仍仅在各自依赖项前要求冻结，未由 AI 自行接受。完整 goal 继续活动，不因本维护项 DONE 而结束。
