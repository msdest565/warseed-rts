# WS-R4-002：行动方案定义、生成与比较

```yaml
work_item_id: WS-R4-002
title: Generate deterministic alternative courses of action
phase: R4
type: planning_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 同一合法局势和玩家目标稳定生成2-3个兵力/路线/投入/风险不同且可审计的行动方案。
why_now: R4-001已提供公平黑板，后续UI和任务图需要具体方案契约。
depends_on: [WS-R4-001]
source_documents: [docs/GAMEPLAY_REWORK_ROADMAP.md, docs/AI_DESIGN_AND_DECISION_TREES.md, docs/TECHNICAL_PLAN.md, docs/work_items/WS-R4-001.md]
current_evidence: [14d4451已推送, R4-001专项/18套/四关smoke全部PASS]
in_scope: [typed规划请求/方案配置/分工/评分DTO, 三类方案生成, 只读宿主入口, 数据校验, 确定性与知识污染测试]
out_of_scope: [玩家批准UI, 命令下发, 阶段任务图, 自动控制归还, 敌方AI, 存档变化, 具体导航执行]
invariants: [只读合法快照, 不抢接管卡, 不提前扣Supply, 10Hz与原执行不变, 无隐藏敌军输入]
behavior_contract:
  inputs: WorldSnapshot、观察阵营、typed StaffPlanRequest（目标地区/最大Supply预算/允许卡ID）和已验证的typed方案配置
  authoritative_changes: none；宿主get_staff_plans只返回方案，不提交命令
  rejection_reasons: 复用黑板输入拒绝；BATTLE_ENDED、INVALID_REQUEST、UNKNOWN_OBJECTIVE、OBJECTIVE_ALREADY_HELD、INVALID_PROFILES、INSUFFICIENT_ALTERNATIVES
  events_and_snapshots: StaffPlanSet含source_tick、observer、来源指纹、请求副本和2-3个StaffCourseOfAction；逐卡分工/目标/轴线/路标、Supply部署成本、投入兵员、预备兵员、路线长度、准备延迟、风险分量、utility与稳定原因
data_contracts:
  - StaffPlanProfile为typed Resource，受限DIRECT/RECON_FIRST/FLANK三个kind；编制比例、准备延迟、风险权重范围校验；不支持表达式和任意参数字典
  - 只使用公开战略地区位置作为路线轴；route_is_navigation_path=false，具体障碍/寻路由R4-004统一命令验证
  - 部队卡为最小分工单位；直接进攻偏主力集中，先侦察留较多预备，侧翼通过另一个地区形成不同路线
  - 部署中的、撤离的、接管的、归队的、组织归零卡不可分配；预备卡仅在请求及当前Supply/人口同时可承担时列入部署投入
  - 不为制造三方案无条件部署预备卡；同成本方案可以通过投入兵员、路程和准备时间展示不同代价
  - 风险使用快照可见/历史接触和报告，不把重叠报告与单体数量相加；未知情报另列惩罚，不视为安全
  - utility是公开工程评分而非获胜概率；不自动选择或下达排名最高方案
  - 相同请求/输入稳定排序、稳定ID和指纹；不足两个可行且不同的方案时具名拒绝，不填充重复或空方案
persistence: {format_change: false, migration: none}
observability: [source_fingerprint, 分量评分, evidence_fact_ids, 方案指纹和值拷贝]
acceptance_criteria:
  - 灰脊开局生成2-3个实际可区分的方案，具有逐卡分工、战略路线、成本、风险与预备兵力。
  - 预算、人口、缺兵、接管、错误目标、非法配置有稳定行为和拒绝；不能重复分配或超预算。
  - 请求风险偏好与真实可见威胁改变评分，隐藏真值污染不改变方案；未知地区有不确定性成本。
  - 同种子、输入换序、旧快照、DTO深复制与宿主无副作用通过；18套及四关smoke无行为回归。
verification:
  baseline: [R4-001的18套/四关通过, git status --short, git diff --check]
  focused: [tests/tools/staff_plans_smoke.gd, tests/test_runner.gd, 四关smoke, 60/80规划耗时]
  full_gate: false
  full_gate_reason: 只读规划不改权威/命令/原快照/地图/存档/UI；接入执行和阶段出口必须完整门。
  optional_research: [方案可理解性在R4-003之后可选]
expected_files: [src/data/staff_plan_*, data/ai/staff_*.tres, src/agents/staff_*, src/app/simulation_host.gd, tests/unit/test_staff_plans.gd, tests/tools/staff_plans_smoke.gd]
risks: [战略路线不是导航可达证明, 效用不是胜率, 无真人方案理解证据]
rollback: 移除只读入口即可回到R4-001行为，无持久化迁移。
handoff_outputs: [验证记录, 状态更新, 下一项WS-R4-003]
```

2026-09-13 READY → DISCOVERY → CONTRACT：核查五字段高层意图、CommanderOrder统一命令、公开地区、控制权与预算。该工作项只建立可审核的方案，不提前实现批准或任务执行。地图轴线明确为战略路标，避免将直线建议冒充可达路径。

CONTRACT → IMPLEMENTING：按三类候选生成器实施。

IMPLEMENTING → VERIFYING：三种typed配置和方案生成已接通只读宿主。首轮测试fixture漏UnitState构造参数已修正；专项focused2通过。审查补充结算后拒绝和预备卡成员接管保护，运行最终专项及18套/四关。

2026-09-13 VERIFYING → REVIEWING → DONE：最终专项artifacts/r4-002-focused-final.log退出0；18套artifacts/r4-002-regression.log及四关smoke全部PASS，无脚本错误。60/80各150次规划P95为1.073/1.306ms（10ms预算），报告artifacts/r4-002-staff-plans.json包含可复查的完整方案样本。旧世界40tick有/无只读规划对照一致。

已核对三类实际不同分工与轴线、真实侦察卡先行/主力准备延迟、逐卡不重分配、部署费用/人口/实际Supply上限、允许卡列表、接管/归队保护、结算后拒绝、真值/错阵营/非法数据拒绝、双方继承的公平黑板及新鲜隐藏敌军污染、可见威胁和风险偏好评分、输入/配置换序、深复制及宿主不消费/不发令。实际diff和git diff --check通过；未修改旧权威、命令、存档和HUD，故本项不重复完整发布/实渲门。

限制：route_distance为各卡战略路标长度合计，不是寻路距离或到达时间；utility为可比较的工程启发式，当前样本集中投入排名较高，不证明最优策略或胜率。中英文方案显示、批准/修改/拒绝属R4-003，阶段执行属R4-004；本项没有自动执行排名第一方案。资料、评分和配置全部离线，不读取敌方计划或隐藏部署。SIMULATED证据不替代可选HUMAN理解研究。唯一下一项WS-R4-003。
