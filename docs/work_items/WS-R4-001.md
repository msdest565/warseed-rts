# WS-R4-001：公平态势评估黑板

```yaml
work_item_id: WS-R4-001
title: Derive a fair staff situation blackboard
phase: R4
type: snapshot_agent_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 参谋只根据本阵营合法快照提供可追溯的威胁、机会、兵力与补给缺口、情报可信度。
why_now: R3工程出口已完成，行动方案生成需要稳定的合法态势输入。
depends_on: [WS-R3-006]
source_documents:
  - docs/GAMEPLAY_REWORK_ROADMAP.md
  - docs/AI_DESIGN_AND_DECISION_TREES.md
  - docs/TECHNICAL_PLAN.md
  - docs/DECISIONS.md
current_evidence:
  - R3最终686.300秒完整门及双语五档通过
  - create_faction_snapshot返回过滤的WorldSnapshot；FactionSnapshot类实际只含阵营经济
  - 既有态势叠层是表现字典，Legacy参谋是生产仲裁，均不直接替代新typed黑板
in_scope: [typed黑板和事实记录, 纯快照评估器, 宿主只读获取入口, 污染/确定性/值拷贝及集成测试, 状态文档]
out_of_scope: [COA生成及比较UI, 任务图与自动改令, 敌方计划, 旧参谋重写, 存档升级, 战斗调参]
invariants: [10Hz不变, 不接受世界真值, 不提交或分配命令, 不改变控制权或资源, 不读取外部配置或网络]
behavior_contract:
  inputs: 经create_faction_snapshot产生的WorldSnapshot及明确observer_faction_id
  authoritative_changes: none；宿主按需提供值拷贝评估，不写世界、不缓存跨局状态
  rejection_reasons: SNAPSHOT_REQUIRED、TRUE_STATE_FORBIDDEN、WRONG_OBSERVER_FACTION、FACTION_KNOWLEDGE_REQUIRED、OBSERVER_FACTION_REQUIRED
  events_and_snapshots: StaffSituationSnapshot、StaffSituationFact、StaffCardAssessment；含source_tick、稳定fact_id、kind/category、reason_key、证据ID、last_observed_tick、confidence_percent
data_contracts:
  - 文档中的FactionSnapshot语义映射到现有阵营过滤WorldSnapshot；不进行无关类重命名
  - 事实和卡牌评估为typed DTO；字典只用于序列化与查询索引，不作内容表达式
  - 当前可见接触可信度100；历史接触从最后已知位置和tick线性衰减至250tick的0，不当成实时敌军
  - 情报报告独立保留估计区间和矛盾来源，不与已知接触相加避免重复计数；矛盾可信度最高25
  - 可夺取地区是候选机会，不宣称安全/可达；无有效情报产生信息缺口；不读取隐藏任务、敌方卡牌或经济
  - 己方卡兵员/组织/弹药缺口可见；玩家接管和归队中的卡不算可分配兵力，预备兵力不算已部署
  - 不排序或修改输入数组；输出按稳定ID排序，重复及换序输入得到同一指纹，旧输出不随输入或新评估改变
persistence: {format_change: false, migration: none}
observability: [canonical_json/fingerprint, reason和来源ID, source_tick, 宿主get_staff_assessment]
acceptance_criteria:
  - 正常灰脊宿主可获得本阵营typed态势，空/真值/错阵营输入具名拒绝。
  - 隐藏敌军位置/生命/任务/经济改变后重新创建合法快照，评估指纹不变；对敌方观察者同样成立。
  - 看见后失联只保留最后已知位置；老化和矛盾报告降低可信度，未探索地区不被宣布安全。
  - 玩家接管卡、预备卡、已部署卡、损员/低组织/缺弹/受阻分别反映正确事实，查询不发命令。
  - 输出稳定排序、深值拷贝、序列化和宿主重开隔离通过，原18套与四关smoke无行为回归。
verification:
  baseline: [R3最终完整发布门PASS, git status --short, git diff --check]
  focused: [tests/tools/staff_situation_smoke.gd, tests/test_runner.gd, 四关smoke, 60/80评估开销]
  full_gate: false
  full_gate_reason: 纯派生黑板和只读宿主入口，不修改共享权威状态、命令、原快照契约、地图、存档或UI；阶段出口仍运行完整门。
  optional_research: [真人对后续方案的信任与理解，R4-003之后可选]
expected_files: [src/agents/staff_*, src/app/simulation_host.gd, tests/unit/test_staff_situation.gd, tests/tools/staff_situation_smoke.gd, docs/AI_DEVELOPMENT_STATE.md]
risks: [可信度为工程启发式而非真实概率, 地区候选不保证路径和收益, 不证明计划完成率或真人纠正负担]
rollback: 本项无持久化数据，移除只读入口即可回到R3行为；保留证据，后续方案不得绕过合法快照。
handoff_outputs: [验证证据, 限制, 状态更新, 唯一下一项WS-R4-002]
```

2026-09-13 READY → DISCOVERY → CONTRACT：已核查快照、情报记忆、卡牌控制、宿主及测试入口，冻结上述纯评估范围。原文FactionSnapshot是语义称谓，当前代码的WorldSnapshot具有is_true_state/observer_faction_id/knowledge边界；采用该现有入口符合TECHNICAL_PLAN，不改变产品行为或迁移格式。

CONTRACT → IMPLEMENTING：开始typed黑板与只读宿主入口。

IMPLEMENTING → VERIFYING：typed事实/卡牌/黑板、评估器和宿主只读入口已实现。首轮测试构造TaskState漏参已修正（仅测试），focused2专项PASS；60/80实体各150次评估P95 0.248/0.343ms，未改变战斗推进。开始18套和四关回归。

2026-09-13 VERIFYING → REVIEWING → DONE：focused2专项PASS；18套完整回归artifacts/r4-001-regression.log正常退出0，无脚本错误；grey_ridge/broken_bridge/fog_forest/black_well四关smoke均PASS。60/80实体各150次纯评估P95 0.248/0.343ms，门为5ms，报告artifacts/r4-001-staff-situation.json。隐藏双方真值污染后重新生成快照结果相同；当前/历史/过期/矛盾情报、来源ID、稳定换序、深值拷贝、接管/归队/预备排除、四种卡缺口及宿主无命令/无花费/重开隔离全部覆盖。

实际diff只增加纯派生评估及只读宿主入口，不改变权威推进、旧快照、命令、存档或HUD；按冻结验证矩阵不重复发布/五档实渲。新增reason_key是机器审计契约，面向玩家的方案文本在R4-003本地化接入。本项未生成行动方案、未改变AI执行、未证明纠正负担或任务完成率。可信度为工程启发式而非概率，机会不保证安全/可达。所有证据SIMULATED，HUMAN仍可选NOT_RUN。源码diff与git diff --check通过，下一项WS-R4-002。
