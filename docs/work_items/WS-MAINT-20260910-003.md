# WS-MAINT-20260910-003：整卡交还 AI 与将领总览

```yaml
work_item_id: WS-MAINT-20260910-003
title: Restore visible whole-card handoff and a complete commander overview
phase: R3
type: control_ui_vertical_slice
status: DONE
owner: ai
external_research: optional
objective: 玩家直接操作后可明确交回 AI，执行决策恢复将领指挥，底部完整总览所有部队。
why_now: 用户复测报告归还不可达、决策无法执行，且将领部队列表依赖滚动。
depends_on: [WS-MAINT-20260910-002, WS-R3-002]
source_documents:
  - docs/AI_DEVELOPMENT_STATE.md
  - docs/AI_DEVELOPMENT_WORKFLOW.md
  - docs/GAME_DESIGN_DOCUMENT.md
  - docs/work_items/WS-MAINT-20260910-002.md
current_evidence:
  - 战术卡片没有控制按钮；原 R 快捷键只在 unhandled input 处理，且旧归还依赖 return_task。
  - CommanderOrder 任务重发保留 PLAYER_OVERRIDDEN，决策目标更新但执行不恢复。
  - 底部固定 196/210 高，三列逐卡堆叠导致滚动。
in_scope:
  - 明确下发将领决策/目标/路线/姿态执行命令时，原子交回该将领部署中的手控卡；拒绝命令不改变控制。
  - R 主动交回当前整卡或选中将领的手控部队；没有旧任务或旧任务失效时仍可接手当前意图。
  - 底部标题区提供所选卡的 AI 接手按钮，左上显示手控提醒及快捷键提示，卡面显示手控/归队状态，暂停中给出排队反馈。
  - 将领区紧凑分组、自适应整卡网格和高度，在当前五档窗口与最多十二卡下无需滚动展示。
out_of_scope: [混成编制和 v4 迁移留给 R3-003, 新 AI 战术和兵种留给原阶段队列]
invariants:
  - SimulationWorld 10 Hz 权威、统一命令验证队列、合法快照与值拷贝。
  - 自动 Agent 更新不得抢回玩家控制；用户下发执行决策构成明确交还授权。
  - 文本输入、菜单、战前和战后不触发战场快捷键；一次按键只提交一次。
behavior_contract:
  inputs: [CommanderOrderCommand, UnitCardControlCommand, R 按键, AI 接手按钮]
  authoritative_changes:
    - 成功的玩家执行决策先交回其将领的手控/归队卡，再创建当前任务；其他将领卡不受影响。
    - 主动归还优先保留有效旧任务的既有归队路径；无效/无旧任务则接手当前将领目标。
    - 同 tick 接管与决策按统一队列顺序执行，暂停中只排队。
  rejection_reasons: [沿用所属阵营、已部署、有效将领与编队校验, INVALID_DISPOSITION]
  events_and_snapshots: [沿用控制事件并标记玩家决策交还原因, UI 只按快照确认]
data_contracts: [稳定将领/部队卡 ID, 无内容格式变化]
persistence: {format_change: false, migration: none}
observability: [左上手控提示, R 交还回执, 逐卡控制状态, 将领汇总与风格]
acceptance_criteria:
  - 接管后下决策恢复该将领 AI，旧/无/已取消任务可归还，拒绝和其他将领隔离通过。
  - R 和按钮真实输入可用，UI 焦点不吞快捷键，文本框/菜单不误触，暂停中正确排队。
  - 五档窗口的当前十二卡可见且可点击，不依赖滚轮，保留兵力/将领汇总/风格/状态文字。
  - 18 套回归、四关、完整策略矩阵、80 实体、五档实渲与完整发布门通过。
verification:
  baseline: [R3-002 完整门 1930.230 秒 PASS，受验源码一致]
  focused: [归还纯模拟与命令顺序测试, 实际 R 和按钮集成, 五档十二卡矩阵]
  full_gate: true
  optional_research: [布局理解和控制交接体验]
expected_files: [src/commands/, src/input/input_controller.gd, src/simulation/core/simulation_world.gd, src/ui/army_board.gd, src/app/game_root.gd, locale/, tests/]
risks: [新决策与旧归队任务竞争, 五将领窄屏空间, 快捷键与文本焦点冲突]
rollback: 恢复本项控制和界面增量，无存档迁移。
handoff_outputs: [正常状态文档和证据, D-021 已接受后继续 WS-R3-003]
```

READY → DISCOVERY → CONTRACT。用户明确授权新执行决策交回 AI，替代此前“决策仅修改意图而保留接管”的行为；保留没有新玩家决定时的接管优先。基线为已验证且源码未改的 R3-002 完整门。D-021 同步 Accepted，R3-003 在本维护闭环后领取；D-022 仍 Deferred。

CONTRACT → IMPLEMENTING → VERIFYING → REWORK → VERIFYING。新增纯模拟覆盖拒绝不交还、限定将领、后台更新不抢回、旧快照不变、同 tick 接管后决策、无旧任务和已取消任务归还。18 套回归已通过。首轮实渲发现文本焦点仍可能落入旧 unhandled R 处理器，已隔离文本输入；窄屏固定留白压缩后保留原地图/决策面积断言，480×800 的真实 R、按钮、批准决策、五将领十二卡可见/点击均通过。最终五档与回归正在连续运行，之后仍须完整发布门。

执行授权使用明确的命令语义：ASSIGN_INTENT 是玩家批准执行；宿主创建的目标/路线/姿态命令携带 `hand_back_control`。旧低层诊断命令可保持只更新目标的语义，因此 R3-002 的冻结黄金轨迹继续原样验收；后台 Agent 即使发出同类更新也不能抢回手控卡。

最终实渲返工记录：五档连续运行时，超出物理显示器的窗口导致模拟鼠标与原生位置不同步；统一点击前移动窗口使目标物理可达并同步鼠标，补员按下/快照刷新/松开的原断言保持。切换分辨率时同步根 Window 尺寸，避免旧窗口尺寸回写。`artifacts/maint-003-ui-final4.log` 连续五档 PASS，正常退出 0；每档包含 R、文本与弹出菜单隔离、按钮交还、批准决策自动接手、十二卡可见和逐卡点击。1280×720 与 480×800 最终十二卡截图已视觉抽查，未放宽原地图面积、遮挡或命令历史断言。证据为 SIMULATED，最终 18 套回归与完整发布门仍待完成。

VERIFYING → REWORK → VERIFYING：最终回归暴露小地图场景遗留的 232px 最小宽度，在同帧从窄屏切回桌面时使实际宽度超过 180px 左栏并侵入地图；移除这个过期场景约束，Legacy 仍按原 offset 尺寸显示。定向隔离验收 `artifacts/maint-003-layout3.log` PASS，原遮挡断言保留并补实际矩形。修复后重新运行最终回归和五档矩阵，完整门尚未启动。

最终分层验证：`artifacts/maint-003-tests-final2.log` 18/18 PASS；`artifacts/maint-003-ui-final6.log` 五档 PASS，均正常退出 0。后一轮补齐暂停按钮悬停的物理定位，游戏源码未改。五档 JSON、桌面/最窄十二卡及暂停交还截图归档于 `artifacts/maint-003-evidence/`，323 个源码/内容/验证文件哈希已冻结。完整发布门已启动，日志 `artifacts/maint-003-release.log`，须等正常结束后才可审查并标记 DONE。

完整门中间证据：两轮 18/18（含隔离会话）、Legacy、灰脊 smoke/开局矩阵均 PASS；5 策略 × 3 敌方计划 × 2 确定性完整对局 PASS，耗时 782.53 秒，JSON 已独立归档。其余关卡、性能与导出仍运行，当前不是完整门完成结论。

## 最终验收（2026-09-10）

VERIFYING → REVIEWING → DONE。完整发布门正常退出 0，耗时 `1487.349s`。两轮 18/18、Legacy/四关 smoke 与矩阵、72 案例、15×2 完整对局、性能、反馈工具、Windows 导出、包内启动及试玩包 smoke 全部 PASS。完整策略指纹仍为 `a994185d975a06e046fe608567e9adb685b6a9525a83fdc85b858477d8a51255`，确定性和质量门通过。

80 实体模拟/表现 P95 `10.472/1.344 ms`，正式门通过；120 实体 `15.429/1.928 ms` 仅作压力证据。五档实渲最终 `maint-003-ui-final6.log` PASS。323 个源码/内容/验证文件与启动门时哈希一致；实际 diff 与 `git diff --check` 已审查，无无关清理、提交、推送、分支或 PR。

EXE SHA-256 `679DF06F7F9F2D2293768747AA9AD71FB996869249960179ADA8180445FC0A7A`；PCK `6178532` bytes / `CAF20D37F891353931E0DF089187F695D92923AA2EBD2DD5AAB3932B2173284C`。证据摘要 `artifacts/maint-003-summary.json`，独立归档 `artifacts/maint-003-evidence/`。所有工程与视觉证据 SIMULATED，真人理解 OPTIONAL_NOT_RUN；无存档格式变化。

唯一下一项为 WS-R3-003，D-021 已 Accepted，契约及设计准备为 `WS-R3-003.md` 和 `../R3_COMPOSITION_AND_SAVE_COMPATIBILITY.md`。维护完成不代表完整 R3/R4/R5 goal 完成；D-022 仍只在 R3-004 前构成产品决定门。
